# 이어서 할 일 — `feat/reel-cut-r2v` (2026-08-25 기준)

이 문서는 **다음 세션이 이 브랜치를 그대로 이어받기 위한 것**이다.
사실은 코드가 진실의 원천이다. 여기 적힌 것과 코드가 어긋나면 코드를 믿고 이 문서를 고쳐라.

---

## 0. 지금 어디에 있나

| | |
|---|---|
| 워크트리 | `C:\Users\fixup\shotform-saas\.claude\worktrees\step-gate` |
| 브랜치 | `feat/reel-cut-r2v` |
| 테스트 | **4,496 그린** (10 skipped) — `npx vitest run` |
| 배포 | **프로덕션 라이브** (fixup-shortform-service.vercel.app) |
| 원격 | `fixup`·`origin` **둘 다 푸시됨** (브랜치만 — main 병합은 아직) |
| 개발 서버 | `npm run dev` → http://localhost:3000 (루트는 `/reel/new` 로 보낸다) |

> ⚠️ **커밋·푸시·배포는 사장님이 요청할 때만.** 2026-08-25 회차는 요청을 받아 다 했다.
>
> ⚠️ **`git checkout <파일>` 을 쓰지 마라.** 진행 중 작업이 사라진다 — 이 저장소에서 한
> 세션에 두 번 밟은 함정이다. 되돌릴 일이 있으면 백업 후 복원해라.
>
> ⚠️ **병렬 세션이 있으면 커밋 직전에 `git branch --show-current` 를 확인해라.**
> 남의 브랜치에 커밋된 사고가 있었다.

### ⚠️ 프로덕션 env — 2026-08-25 에 하나 바꿨다

`FAL_IMAGE_ENDPOINT` 를 **`openai/gpt-image-2`** 로 교체했다(12일 전 값이 남아 있어
스토리보드를 nano-banana 로 굽고 있었다 — 그러면 격자 치수(imageSize)가 안 실려 칸이
깨진다). 되돌릴 일이 생기면 `fal-ai/nano-banana-2` 다.
★ `SHOTFORM_NO_CREDITS` 는 **프로덕션에도 있다** — 크레딧이 꺼져 있다.

---

## 1. 값이 나가는 일 — 승인 없이 하지 마라

이 회차는 **오프 모드**로 돌았다. 사장님이 명시한 조건이다.

```bash
SHOTFORM_FAKE=fal SHOTFORM_NO_CREDITS=1 npm run dev
```

- `SHOTFORM_FAKE=fal` — fal 호출이 가짜로 떨어진다. 이미지는 `/samples/storyboard-*.jpg`,
  영상은 `/samples/reel-15s.mp4` 로 돌아온다(칸 수를 보고 고른다).
- **실제 이미지·영상 생성은 실행 전 반드시 사장님 검토를 받는다.** 무료로 볼 수 있는 것
  (테스트·가짜 모드·로컬 ffmpeg·OpenAI 단독)을 먼저 다 한다.

### 🔴 정가 재산정은 **사장님이 요청할 때만** 한다

사장님이 명시적으로 미뤘다. 화질·모델·길이가 값을 바꾸는 것은 알고 있지만,
**요청 없이 값 계산이나 크레딧 표를 건드리지 마라.**

### 🔴 그때 반드시 같이 볼 것 — 컷별 갈래의 돈 구멍

컷 수를 LLM 이 고르게 하면서 **`percut` 갈래의 원가가 컷 수에 비례해 늘어난다.**
컷 최소가 4초라 컷을 늘리면 굽는 총 길이가 목표 길이를 넘어간다. 청구는 목표 길이로 한다:

⚠️ **이 표의 값은 화질마다 다르다.** 예전 판은 화질을 안 적어서 480p 값을 절대값처럼
읽게 했다 — **기본은 720p 라 실제로는 두 배가 넘는다.** 값은 `lib/costs.js` 의
`estimateCost` 하나가 낸다(Seedance 2.0: 480p $0.1348/s · 720p $0.3034/s · 1080p $0.682/s).

| 15초 영상 | 굽는 길이 | 원가(480p) | **원가(720p·기본)** | 청구 |
|---|---|---|---|---|
| 통짜 | 15초 | $2.02 | **$4.55** | 40크레딧 |
| 3컷 | 15초 | $2.02 | **$4.55** | 40크레딧 |
| 6컷 | 24초 | $3.24 | **$7.28** | 40크레딧 |
| **12컷** | **48초** | **$6.47** | **$14.56** | 40크레딧 |

**720p 12컷이면 편당 $14.56 을 쓰고 40크레딧을 받는다.** 화면 결과는 `lib/compose.js` 의
`trim=duration=` 이 잘라 내서 멀쩡하다 — **그래서 눈으로는 안 보인다.**

> 통짜(`oneshot`)는 이 구멍이 없다(한 번에 15초 한 판). 구멍은 통짜 조건을 못 맞춰
> `percut` 으로 떨어질 때 열린다.

---

## 2. 다음 세션이 바로 집어야 할 것 (우선순위 순)

### ① ~~⑤영상 화면의 진행 표시~~ → **완료(2026-08-25)**

도는 표시 + 어디까지 왔는지(통짜는 "한 편을 굽는 중", 컷별은 "컷 n/m")를 띄우고,
아직 안 나온 칸 위에는 덮개(`.frame-busy`)가 돈다. 만든 영상도 이 화면에서 튼다.

⚠️ **남은 것 하나** — "멈춘 것 같아요"는 아직 못 가른다. `generationState` 를 쓰려면
`app/api/reel/[id]/status/route.js` 가 `progress` 와 `stalled_for_ms` 를 실어야 하는데
그 계약이 지금 없다(reel·광고는 단계별 흐름의 다섯 상태 라우트 계약 밖이다).
실어 보내게 되면 그때 `generationState` 로 옮긴다 — 베낄 본은 `app/create/[id]/video/page.js`.

### ①-2 · ①-3 · ①-5 ~~화면 셋~~ → **완료(2026-08-25)**

사장님이 실검증 중에 짚은 것들이고 그 자리에서 다 고쳤다.

| | 무엇이었나 | 어떻게 |
|---|---|---|
| ①-2 | ②시나리오·③이미지·⑥완성에 **로딩 표시가 없었다**(⑤영상만 되어 있었다) | 네 화면이 같은 규칙을 쓴다 — `tests/reel-busy-ui.test.js` 가 잰다 |
| ①-2-b | [다시 쓰기]가 `busy ? null` 로 **버튼 자리를 비웠다** | 그 자리에 "쓰는 중…" 을 남긴다(버튼이 아니라 **글**) |
| ①-2-c | ⑥완성은 누른 직후 **아무 표시도 없는 구간**이 있었다 | 문구가 `busy` 도 함께 본다 |
| ①-3 | ③이미지에 **⑤영상의 오류**가 떴다 | `reelErrorFor(reel, step)` 하나가 가른다 |
| ①-4 | 다시 구워도 **옛 영상**이 보였다 | 각인(`video.ts`)을 남기고 주소에 `?v=` 를 싣는다 |
| ①-5 | ⑥완성에 **내려받기 버튼이 없었다** | `?dl=1` 링크를 보관함 링크 **앞**에 둔다 |

★★ **패턴이 하나였다** — reel 완성 화면을 만들 때 `app/create/[id]/done/page.js` 에서
**자막 편집기만 가져오고 그 주변 장치를 안 가져왔다**(캐시 무력화·내려받기). 앞으로
reel 에 화면을 더할 때는 그 파일을 나란히 놓고 **빠진 것을 훑어라.**

★ ①-3 의 뿌리는 남아 있다: reel 은 오류 필드가 **하나**다(`reel.error`). 단계별 흐름은
필드를 갈라서 푼다(`lib/step-errors.js` 의 `STEP_ERROR_FIELDS`). 지금은 "누가 쓴
오류인가"(`errorStep`)를 함께 적어 읽는 쪽에서 가르는 방식이라, 저장 모양을 안 바꿔
옛 문서가 안 깨진다. 필드를 제대로 가르려면 그 표를 빌리면 된다.

### ② ⑤영상 자동 생성 여부가 미결

사장님에게 물어야 한다. 세 갈래:

- **A** — 버튼만 두고 진행 표시만 개선 (지금 흐름 유지)
- **B** — 화면에 들어오면 자동으로 굽는다 (②시나리오처럼)
- **C** — 가짜 모드에서만 자동

⚠️ **B 는 돈이 나가는 자동화다.** ②시나리오가 자동인 이유는 무료라서다.

### ③ 관리자 이력 페이지 — 설계만 있고 착수 0

사장님 요청: *"관리자는 사용자 누가 결과물을 만들고 진행하는지 이력을 볼 수 있는 페이지."*
합의된 모양은 **"작품 한 줄"** — 사용자·제목·단계·시각을 한 줄로.

### ④ `look` 글자 금지 규칙 — 미구현

스토리보드 이미지에 뜻 없는 글자가 섞여 나온 것을 **세 번** 봤다. 이미지 지시문에
글자 금지를 넣기로 했으나 아직 안 넣었다.

---

## 3. 알고도 남겨 둔 것 (리뷰가 잡았다)

| 자리 | 무엇 |
|---|---|
| `app/api/reel/[id]/render/route.js` | `composeVideo` 가 주는 `rawUrl` 을 버린다 |
| ~~`lib/compose.js` 길이 부풀림~~ | **고쳤다(08-25)** — 15초짜리가 29초로 찍혔다. `composeSeconds` |
| ~~스토리보드가 레퍼런스를 안 싣는다~~ | **고쳤다(08-25)** — 첨부 사진이 안 가서 **다른 제품**이 그려졌다 |
| `reel` 자막 | 자막 언어를 고르는 화면이 없다(음성 언어만 있다 — 지금은 그것을 따라간다) |
| **반올림이 컷 초 합을 부풀린다** | 15초 목표에 합 17초(`buildReelCuts` 의 `Math.round`). 지금은 합성이 비례로 눌러 담아 화면은 맞지만, **시나리오 단계에서 맞추는 것이 근본**이다 |
| **fal 초상 검사** | 스토리보드에 **정면으로 크게 웃는 얼굴**이 여럿이면 r2v 가 422 로 거절한다(2026-08-25 실측). 통과한 표본은 얼굴이 1/4컷·눈 감음·측면광이었고, 막힌 표본은 3/5컷·정면·카메라 응시였다. **끄는 파라미터는 없다**(fal 문서 확인) — 얼굴을 내리거나 화풍을 바꾸는 수밖에 없다 |

---

## 4. 이 회차에 배운 것 — 다시 밟지 마라

### 값·모델

- **GPT Image 2 는 프리셋만 보면 1024px 이 최대처럼 보인다.** 실제로는
  `image_size` 를 **객체**(`{width, height}`)로 주면 2160×3840 까지 간다.
  프리셋 목록만 보고 "fal 에서는 불가능"이라고 두 번 틀렸다.
- `quality`(low/medium/high)는 **해상도가 아니라 품질**을 정한다.
- **Seedance 2.0 은 컷 최소 4초**다(`lib/clip-limits.js`). 3초는 Kling v3 값이다.

### 스토리보드

- 격자 칸은 9:16, **전체 비율이 프리셋에 맞아떨어져야** 한다.
  성립하는 칸 수: **3·4·6·9·10·12·16**. 20칸은 칸이 굽는 해상도의 0.93배로 작아져 깨진다
  (16칸이 1.07배로 아슬아슬하게 남는다 — 이것이 상한의 근거다).
- ⚠️ **모델마다 칸 상한이 다르다.** nano-banana 2 는 4K(3072×5504)라 16칸까지 가지만,
  **GPT Image 2 는 2160×3840 이 최대라 9칸이 상한**이다. 지금 기본은 GPT Image 2 다.
- ⚠️ **`progress.md` Ruling 20 이 낡았다** — 거기는 `3·4·6·9·12` 라 적혀 있는데
  코드 표(`REEL_GRIDS`)에는 **10 과 16 이 더 있다**. 코드가 맞다.
- 칸 수는 표에 박지 않고 **LLM 시나리오가 고른다**(`lib/reel/scenario-rules.js`).
  사장님 뜻: *"컷 수는 필요한만큼 사용해 — 전체 시나리오 구성에 따라 다르다."*

### ★ 통짜(r2v)가 컷별보다 낫다

스토리보드 **한 장 + 프롬프트 하나**를 r2v 로 넘기면 이음매 없는 15초가 나온다.
컷별로 굽고 붙이는 것보다 **싸고 정확하다**. 판정은 `lib/reel/oneshot.js` 의
`planReelBake(project)` 하나가 쥔다(`oneshot` / `percut`).

### 자막 — 타이밍은 whisper, 글자는 시나리오

- 모델이 글자를 **잘못 읽는다**("끓이기만" → "끄기만"). 그래서 whisper 결과를 자막
  텍스트로 쓰면 안 된다. **시각만** 가져오고 글자는 시나리오 것을 쓴다(`lib/speech-timing.js`).
- 어긋남은 ±2초인데 **방향이 일정하지 않다** → 상수 보정은 불가능하다.
- 통짜 갈래에서 자막 2·3번이 **영상 밖**(15초 영상에 18초·24초)으로 나간 적이 있다.
  `needsSpeechProbe` 가 통짜를 못 알아본 것이 원인 — `video.whole` 플래그로 고쳤다.

### 자막 폰트 — 함정 둘

1. **폴더가 둘이다.** 브라우저는 `public/fonts/`, ffmpeg 는 `assets/`. 한쪽만 넣으면
   **미리보기는 맞고 구운 영상만 기본 폰트**가 된다.
2. **`SUBTITLE_FONTS[].family` 는 파일의 `name` 테이블 이름과 글자 그대로 같아야 한다.**
   구글 폰트 표시 이름을 적었다가 틀렸다: `Nanum Myeongjo`(×) → `NanumMyeongjo`(○),
   `Nanum Pen Script`(×) → `Nanum Pen`(○). 틀리면 ffmpeg 가 **조용히** 기본 폰트로 떨어진다.
   `tests/assets.test.js` 가 지금 이것을 잰다.

폰트는 여섯이고 **전부 SIL OFL 1.1** 이다. 근거는 `assets/SUBTITLE-FONTS-LICENSE-NOTE.md`
에 남겼다(NanumMyeongjo 는 파일 안에 OFL 문구가 없어 배포되는 `OFL.txt` 를 따로 확인했다).

### 도구 함정

- **heredoc 이 역슬래시를 먹는다.** `\n` 이 진짜 줄바꿈이 되어 JS 문자열을 깨뜨린다
  (이 회차에 5번 이상 밟았다). `chr(92)` 로 조립하거나 python 으로 써라.
- **한글을 bash payload 로 넘기지 마라.** python `io.open(..., encoding="utf-8")` 을 써라.
- **`.next` 가 두 번 깨졌다.** vitest 와 dev 서버가 동시에 돌 때 EPERM 이 난다.
  고치는 법: dev 서버 끄고 → `rm -rf .next` → **출력을 파일로** 돌려 재기동
  (백그라운드 파이프가 끊기면 워커가 죽는다).
- JSX 파일의 문법 검사는 vitest import 가 아니라 이것으로 해라:
  ```bash
  node -e "require('next/dist/compiled/babel/core').parseSync(require('fs').readFileSync('경로','utf8'),{parserOpts:{plugins:['jsx']}})"
  ```
- **`/samples/` 가 미들웨어에 막혀 307 이 났다.** 매처 제외 목록에 `samples/` 를 넣어 뒀다.

---

## 5. 화면 규칙 — 지금 이렇게 정해져 있다

사장님이 회차 내내 다듬은 것들이다. 새로 화면을 만들 때 **이 규칙을 따라라.**

### 버튼 자리 (2026-08-25 통일)

- **⑤영상의 [영상 만들기]도 그 줄 안**이다(2026-08-25 사장님 지적으로 뒤집혔다).
  옛 규칙은 "돈 나가는 버튼은 그 줄에 혼자 선다"였고 그래서 선 없는 줄에 따로 세웠는데,
  그러면 **구분선 위**에 있게 된다. 지금은 [이전으로]와 같은 줄, 선 아래다.
  ★ 선 없는 갈래(`.step-actions--bare`)는 쓰는 곳이 0 이 되어 지웠다.
- **되돌아가는 버튼은 이름이 늘 "이전으로"**이고 **그 화면 맨 아래 왼쪽 끝**에 선다.
  그리는 곳은 `components/ReelBack.jsx` **하나다** — 화면마다 손으로 적지 마라.
  (이전에는 `← 시나리오로`처럼 앞 단계 이름을 빌려서 화면마다 이름이 달랐다.)
- **프롬프트를 고치는 버튼은 그 프롬프트 칸 안 오른쪽 아래**(`.note-act`)에 선다.
- **안내문은 그 버튼 바로 왼쪽**(`.note-hint`)이다 — 줄의 왼쪽 끝이 아니다.
  둘이 같이 오른쪽으로 붙어 한 덩어리로 읽힌다.
- **굽는 버튼에는 화살표를 안 붙인다** — 화살표는 "다음 화면으로 간다"는 뜻이다.
  ③⑤의 [다시 만들기] · ⑥의 [이대로 완성하기]가 그렇다.
- **⑥완성의 실행줄은 하나다** — 왼쪽 끝이 [이전으로], 오른쪽이 실행이다.
- 다음으로 가는 버튼은 오른쪽(`.fwd`).
- **⑤영상의 [영상 만들기]는 그 줄에 혼자 선다** — 돈이 나가는 버튼이라 되돌아가는
  링크와 나란히 두면 둘 다 "지금 할 일"처럼 읽힌다.
- 잰다: `tests/reel-button-places.test.js`

### 말투·문구

- **값·크레딧·무료를 말하지 않는다.** "무료예요", "크레딧이 나가요", "n번 남았어요",
  "이 한 장을 통째로 넘겨" 같은 부연은 **전부 뺐다**. 값을 말하는 자리는 실제로 돈이
  나가는 ⑤영상 하나뿐이다.
- 설명은 **사장님이 겪는 흐름**으로 쓴다. 파일·함수·필드 이름으로 쓰지 마라.
- 수정 요청은 **한국어로 적는다**(②③④⑤ 모두 같은 모양의 칸). 시나리오 본문은 영어,
  **음성 대사만 한국어**다.

### 흐름

- 루트(`/`)는 `/reel/new` 로 보낸다. 사이드바에는 **영상 만들기(reel)와 광고(ad)만** 뜬다
  (`SIDEBAR_FLOWS`, `components/Sidebar.jsx`). 단계별·수정 흐름은 숨겼다.
- ①~⑥ 단계는 **사이드바**에 있다(메인 화면이 아니다).
- ②시나리오는 **자동으로 생성된다**(무료라서). 굽기는 자동이 아니다.

---

## 6. 이 회차에 새로 생긴 파일

| 파일 | 무엇 |
|---|---|
| `lib/reel/scenario-rules.js` | 격자 표(`REEL_GRIDS`)와 칸 수 규칙 — LLM 에 넘기는 지시문의 근거 |
| `lib/reel/oneshot.js` | 통짜/컷별 판정(`planReelBake`), 전체 프롬프트, 낡음 판정 |
| `lib/reel/storyboard.js` | 격자 크기 계산, 칸 잘라내기(sharp), 업로드 |
| `lib/speech-timing.js` | whisper 결과를 컷에 **순서대로** 물린다. 글자는 안 건드린다 |
| `lib/speech-probe.js` | whisper 호출. 가짜 모드에서는 건너뛰고 **절대 안 던진다** |
| `lib/josa.js` | 받침 판정·조사(`euroRo`). ★ 이전 버튼 이름이 고정되며 화면에서는 안 쓴다 |
| `components/ReelBack.jsx` | 되돌아가는 버튼 — **한 곳** |

---

## 7. 세션을 마칠 때

`C:\Users\fixup\shotform-saas\CLAUDE.md` 의 규칙대로 **wiki 에 반영한다**
(`C:\Users\fixup\obsidian_jaechan`). 사장님이 따로 요청하지 않아도 한다.
wiki 파일은 **Write/Edit 도구로만** 쓴다 — PowerShell 로 쓰면 한글이 깨진다.
