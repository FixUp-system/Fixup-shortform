# 크레딧 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 앱이 "이 사람이 얼마나 더 만들 수 있는가"를 알고, 잔액이 없으면 유료 생성을 막는다.

**Architecture:** 차감 트랜잭션을 새로 만들지 않는다 — **잔액 = `sum_grants(user)` − `sum_costs(actor)`**. 모든 유료 호출이 이미 `cost_records` 에 기록되므로 그 기록이 곧 차감이다. 새로 만드는 것은 충전 장부(`credit_grants`) 하나와 게이트 둘(시작 게이트 402 · `assertBudget` 의 사용자 상한 → 잔액)이다.

**Tech Stack:** Next.js 15 (App Router, JS), Vitest 4, Supabase Postgres(테스트는 메모리 스토어), raw fetch.

## Global Constraints

- 스펙: `docs/superpowers/specs/2026-08-05-credits-design.md`
- **합계는 반드시 SQL 함수가 낸다** — 앱에서 행을 받아 더하면 PostgREST 행 상한(기본 1000)에 걸려 조용히 일부만 더한다(`sum_costs` 에서 이미 겪은 함정)
- **잔액 컬럼을 두지 않는다** — 원장과 갈라질 자리를 만들지 않는다
- 전역(`SHOTFORM_BUDGET_TOTAL_USD`)·프로젝트(`SHOTFORM_BUDGET_PROJECT_USD`) 상한은 **회사 안전핀으로 그대로** 둔다. 바뀌는 것은 `limitUser()` 뿐
- `lib/i2v.js`·`lib/imagegen.js`·`lib/tts.js` 의 `assertBudget` 호출부는 **건드리지 않는다**
- 시작 잔액 0(자동 지급 없음) · 충전은 운영자 수동 · 편수 표시는 `Math.floor`
- 런타임 의존성 추가 금지, UI·메시지·주석 전부 한국어
- 테스트는 `npx vitest run` 전체 그린 유지. 커밋은 태스크마다. 커밋 전 `git branch --show-current` 로 `feature/video-compose` 확인, `git add` 는 명시 경로만
- ⚠️ dev 서버 켠 채 `npm run build` 금지. 3000 포트는 사용자 소유

## 파일 구조

| 파일 | 역할 |
|---|---|
| Modify `db/schema.sql` | `credit_grants` 테이블 + `sum_grants()` 함수 + RLS |
| Modify `lib/store/supabase.js` | `sumGrants(userId)` · `insertGrant(row)` · `listGrantsFor(ids)` |
| Modify `lib/store/memory.js` | 같은 셋(테스트용, 같은 계약) |
| Create `lib/credits.js` | 잔액·편수 환산·시작 게이트 판정(순수 로직 + 스토어 조회) |
| Modify `lib/costs.js:117-119` | `limitUser()` 를 잔액 기반으로 |
| Create `app/api/credits/route.js` | `GET` — 내 잔액(사이드바용) |
| Create `app/api/admin/users/[id]/credits/route.js` | `POST` — 운영자 충전 |
| Modify `app/api/projects/[id]/auto/route.js` | 시작 게이트(한 편치) |
| Modify `/voice`·`/images`·`/clips`·regen 3종 route.js | 시작 게이트(잔액 > 0) |
| Modify `components/Sidebar.jsx:107-112` | `.credit-box` 를 실제 잔액으로 |
| Modify `components/QuickCreate.jsx` | 잔액 부족 시 [영상 만들기] 비활성 + 안내 |
| Modify `app/admin/page.js` | 잔액 열 + [크레딧 넣기] |
| Test | `tests/credits.test.js` · `tests/credits-gate.test.js` · `tests/credits-admin.test.js` · `tests/credits-ui.test.js` |

---

### Task 1: 저장 계층 — `credit_grants` 와 `sum_grants`

**Files:**
- Modify: `db/schema.sql` (끝에 추가)
- Modify: `lib/store/supabase.js` (`sumCosts` 아래에 3개 메서드)
- Modify: `lib/store/memory.js` (`sumCosts` 아래에 3개 메서드)
- Test: `tests/credits.test.js` (신규, describe "스토어 — 충전 장부")

**Interfaces:**
- Consumes: 없음
- Produces: 두 스토어가 같은 계약으로 구현한다 —
  - `sumGrants(userId) → Promise<number>` (충전 합계 USD, 없으면 0)
  - `insertGrant({ user_id, amount_usd, reason, granted_by }) → Promise<void>`
  - `listGrantsFor(userIds: string[]) → Promise<Map<userId, number>>` (사용자별 충전 합계 — `/admin` 목록이 행마다 왕복하지 않게)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/credits.test.js`:

```js
// 충전 장부 — 잔액의 절반(나머지 절반은 기존 cost_records)이다.
// 메모리 스토어로 계약을 못 박는다. Supabase 쪽은 같은 시그니처를 구현하되
// 합계를 SQL 함수(sum_grants)에 맡긴다 — 앱에서 더하면 행 상한에 걸린다.
import { describe, it, expect, beforeEach } from "vitest";
import { resetMemoryStore } from "../lib/store/memory.js";
import { getStore } from "../lib/store/index.js";

const A = "00000000-0000-4000-8000-00000000000a";
const B = "00000000-0000-4000-8000-00000000000b";
const ADMIN = "00000000-0000-4000-8000-0000000000ad";

describe("스토어 — 충전 장부", () => {
  beforeEach(() => resetMemoryStore());

  it("충전이 없으면 합계는 0", async () => {
    expect(await getStore().sumGrants(A)).toBe(0);
  });

  it("충전을 더해서 돌려준다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 2.59, reason: "체험 1편", granted_by: ADMIN });
    await store.insertGrant({ user_id: A, amount_usd: 5, reason: "유료 충전", granted_by: ADMIN });
    expect(await store.sumGrants(A)).toBeCloseTo(7.59, 6);
  });

  it("음수 충전(회수)도 반영된다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 5, reason: "유료 충전", granted_by: ADMIN });
    await store.insertGrant({ user_id: A, amount_usd: -2, reason: "정정", granted_by: ADMIN });
    expect(await store.sumGrants(A)).toBeCloseTo(3, 6);
  });

  it("남의 충전은 안 센다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: B, amount_usd: 10, reason: "유료 충전", granted_by: ADMIN });
    expect(await store.sumGrants(A)).toBe(0);
  });

  it("listGrantsFor 는 사용자별 합계를 한 번에 준다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 3, reason: "충전", granted_by: ADMIN });
    await store.insertGrant({ user_id: A, amount_usd: 2, reason: "충전", granted_by: ADMIN });
    await store.insertGrant({ user_id: B, amount_usd: 1, reason: "충전", granted_by: ADMIN });
    const m = await store.listGrantsFor([A, B, "없는-id"]);
    expect(m.get(A)).toBeCloseTo(5, 6);
    expect(m.get(B)).toBeCloseTo(1, 6);
    expect(m.get("없는-id") ?? 0).toBe(0);
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run tests/credits.test.js` / Expected: FAIL — `store.sumGrants is not a function`

- [ ] **Step 3: 구현** — `db/schema.sql` 끝에 추가:

```sql
-- ── 크레딧 충전 장부 ────────────────────────────────────────────────────
-- 잔액을 컬럼으로 두지 않는다: 잔액 = sum_grants(user) - sum_costs(null, user).
-- 원장(cost_records)이 곧 차감이라 두 값이 갈라질 자리가 없다.
-- 이 테이블은 감사 로그이기도 하다 — 누가(granted_by) 언제 왜(reason) 넣었는지가 남는다.
create table if not exists credit_grants (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid not null references auth.users(id) on delete cascade,
  amount_usd  numeric not null,          -- 양수=충전, 음수=회수(운영자 정정)
  reason      text not null,
  granted_by  uuid not null,
  created_at  timestamptz not null default now()
);
create index if not exists credit_grants_user on credit_grants (user_id);

-- 합계는 DB 가 낸다. 앱에서 행을 받아 더하면 PostgREST 행 상한(기본 1000)에 걸려
-- 조용히 일부만 더한다 — sum_costs 가 이미 겪은 함정이고, 여기서는 잔액이 부풀어
-- 크레딧이 없는 사람에게 생성을 열어 주는 모양이 된다.
create or replace function sum_grants(p_user_id uuid)
returns numeric language sql stable as $$
  select coalesce(sum(amount_usd), 0) from credit_grants where user_id = p_user_id;
$$;

alter table credit_grants enable row level security;  -- 정책 0개 = 전부 거부(앱은 service_role)
```

`lib/store/memory.js` — `sumCosts` 아래에 추가(파일 상단 어딘가의 `const costs = new Map()` 옆에 `const grants = []` 를 두고, `resetMemoryStore` 가 비우게 한다):

```js
  // ── 크레딧 충전 장부 ────────────────────────────────────────
  async insertGrant(row) {
    grants.push({ created_at: new Date().toISOString(), ...clone(row) });
  },
  async sumGrants(userId) {
    return grants.reduce((s, g) => (g.user_id === userId ? s + (Number(g.amount_usd) || 0) : s), 0);
  },
  async listGrantsFor(userIds) {
    const want = new Set(userIds);
    const out = new Map();
    for (const g of grants) {
      if (!want.has(g.user_id)) continue;
      out.set(g.user_id, (out.get(g.user_id) || 0) + (Number(g.amount_usd) || 0));
    }
    return out;
  },
```

`lib/store/supabase.js` — `sumCosts` 아래에 추가:

```js
  // ── 크레딧 충전 장부 ────────────────────────────────────────
  async insertGrant(row) {
    const { error } = await db().from("credit_grants").insert(row);
    if (error) raise(error, "크레딧 충전");
  },

  // 합계는 SQL 함수가 낸다(db/schema.sql 의 sum_grants). sumCosts 와 같은 이유로
  // 숫자로 못 읽으면 던진다 — 조용히 0 을 돌려주면 잔액이 사라져 멀쩡한 사용자가 막히고,
  // 조용히 적게 세면 반대로 없는 크레딧이 생긴다.
  async sumGrants(userId) {
    const { data, error } = await db().rpc("sum_grants", { p_user_id: userId });
    if (error) raise(error, "크레딧 합계");
    const n = data == null ? NaN : Number(data);
    if (!Number.isFinite(n)) {
      throw new Error(
        "크레딧 합계 실패: 합계를 숫자로 읽지 못했어요 — db/schema.sql 의 sum_grants 함수가 올라갔는지 확인해 주세요"
      );
    }
    return n;
  },

  // 목록 화면용 — 행마다 sum_grants 를 부르면 사용자 수만큼 왕복한다.
  // 여기서는 행을 받아 JS 로 더하는데, 그래도 되는 이유는 **한 화면의 사용자들** 것만
  // 받기 때문이다. 다만 행 상한은 여전히 존재하므로 count 로 검산한다.
  async listGrantsFor(userIds) {
    const out = new Map();
    if (!userIds?.length) return out;
    const { data, error, count } = await db()
      .from("credit_grants")
      .select("user_id, amount_usd", { count: "exact" })
      .in("user_id", userIds);
    if (error) raise(error, "크레딧 목록");
    const rows = data || [];
    if (count != null && rows.length < count) {
      throw new Error(`크레딧 목록 실패: ${count}건 중 ${rows.length}건만 받았어요`);
    }
    for (const r of rows) {
      out.set(r.user_id, (out.get(r.user_id) || 0) + (Number(r.amount_usd) || 0));
    }
    return out;
  },
```

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run tests/credits.test.js tests/store-memory.test.js tests/store-actor-sum.test.js` / Expected: PASS
- [ ] **Step 5: Commit** — `git add db/schema.sql lib/store/memory.js lib/store/supabase.js tests/credits.test.js && git commit -m "feat: 크레딧 충전 장부 — 잔액은 컬럼이 아니라 두 합계의 차"`

---

### Task 2: `lib/credits.js` — 잔액과 편수

**Files:**
- Create: `lib/credits.js`
- Test: `tests/credits.test.js` (describe "잔액" 추가)

**Interfaces:**
- Consumes: `getStore().sumGrants(userId)` · `getStore().sumCosts({ actor })` (Task 1 / 기존)
- Produces:
  - `PER_VIDEO_USD` — 상수(env `SHOTFORM_PER_VIDEO_USD`, 기본 `2.59`). **함수로 읽는다**: `perVideoUsd() → number`
  - `balanceFor(userId) → Promise<number>` (USD, 음수 가능)
  - `videosLeft(balanceUsd) → number` (편수, `Math.floor`, 음수는 0)
  - `assertCanStart(userId, { need }) → Promise<void>` — 부족하면 `NoCredits` 를 던진다. `need` 는 USD(자동 관통은 `perVideoUsd()`, 단계별은 아주 작은 값)
  - `class NoCredits extends Error` — `name === "NoCredits"`, `balance` 필드

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/credits.test.js` 에 추가:

```js
import { balanceFor, videosLeft, perVideoUsd, assertCanStart, NoCredits } from "../lib/credits.js";

describe("잔액", () => {
  beforeEach(() => resetMemoryStore());

  it("충전에서 쓴 것을 뺀 값이다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 10, reason: "충전", granted_by: ADMIN });
    await store.addRecord({
      request_id: "r1", ts: 1, endpoint: "fal-ai/x", stage: "영상",
      actor: A, est_cost_usd: 2.5, status: "done",
    });
    expect(await balanceFor(A)).toBeCloseTo(7.5, 6);
  });

  it("남이 쓴 것은 내 잔액을 안 깎는다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 10, reason: "충전", granted_by: ADMIN });
    await store.addRecord({
      request_id: "r2", ts: 1, endpoint: "fal-ai/x", stage: "영상",
      actor: B, est_cost_usd: 4, status: "done",
    });
    expect(await balanceFor(A)).toBeCloseTo(10, 6);
  });

  it("초과하면 음수가 된다 — 병렬 호출이 조금 넘길 수 있고 그것을 숨기지 않는다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 1, reason: "충전", granted_by: ADMIN });
    await store.addRecord({
      request_id: "r3", ts: 1, endpoint: "fal-ai/x", stage: "영상",
      actor: A, est_cost_usd: 1.2, status: "done",
    });
    expect(await balanceFor(A)).toBeLessThan(0);
  });

  it("편수는 내림이고 음수는 0", () => {
    expect(videosLeft(perVideoUsd() * 3 + 0.9)).toBe(3);
    expect(videosLeft(perVideoUsd() - 0.01)).toBe(0);
    expect(videosLeft(-5)).toBe(0);
  });

  it("assertCanStart 는 모자라면 NoCredits 를 던진다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 0.5, reason: "충전", granted_by: ADMIN });
    await expect(assertCanStart(A, { need: perVideoUsd() })).rejects.toMatchObject({ name: "NoCredits" });
  });

  it("assertCanStart 는 충분하면 조용히 통과한다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 100, reason: "충전", granted_by: ADMIN });
    await expect(assertCanStart(A, { need: perVideoUsd() })).resolves.toBeUndefined();
  });
});
```

⚠️ `addRecord` 의 실제 필수 필드는 `lib/store/memory.js` 의 구현을 보고 맞춰라(위 픽스처가 어긋나면 **픽스처를 실물에 맞추고** 스토어를 고치지 마라).

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run tests/credits.test.js` / Expected: FAIL — `lib/credits.js` 없음

- [ ] **Step 3: 구현** — `lib/credits.js`:

```js
// 크레딧 — 잔액은 저장하지 않는다. **충전 합계 − 쓴 합계**가 곧 잔액이다.
//
// 이 결정이 "쓴 만큼 차감"과 "두 입구(자동 관통·단계별)가 같은 자를 쓴다"를 공짜로
// 성립시킨다. 유료 호출은 전부 cost_records 에 남고 request_id 가 멱등키라
// 이중 차감도 이미 막혀 있다.
//
// costs.js 가 이 모듈을 부르고(limitUser), 이 모듈은 costs.js 를 부르지 않는다 —
// 순환 import 를 만들지 않으려고 스토어에서 직접 읽는다.
import { getStore } from "./store/index.js";

// 편수 표시용 기준가. 30초 한 편 실측이 $2.59(클립이 81%)였다.
// env 로 두는 이유는 모델·길이가 바뀌면 이 값도 바뀌기 때문이다. 매번 읽는다 —
// 모듈 로드 시점에 굳히면 테스트가 값을 못 바꾼다(costs.js 의 상한들과 같은 규칙).
export function perVideoUsd() {
  const n = Number(process.env.SHOTFORM_PER_VIDEO_USD ?? 2.59);
  return Number.isFinite(n) && n > 0 ? n : 2.59;
}

export class NoCredits extends Error {
  constructor(balance) {
    super("크레딧이 모자라요 — 운영자에게 문의해 주세요");
    this.name = "NoCredits";
    this.balance = balance;
  }
}

export async function balanceFor(userId) {
  const store = getStore();
  const [granted, spent] = await Promise.all([
    store.sumGrants(userId),
    store.sumCosts({ actor: userId }),
  ]);
  return granted - spent;
}

// 화면에 보이는 숫자. 내림인 이유는 "3편 남았어요"를 보고 눌렀는데 두 편 반이어서
// 중간에 멈추는 일이 없어야 하기 때문이다. 음수(조금 초과한 상태)는 0 으로 보여준다.
export function videosLeft(balanceUsd) {
  const n = Math.floor((Number(balanceUsd) || 0) / perVideoUsd());
  return n > 0 ? n : 0;
}

// 시작 게이트. 유료 흐름을 **시작하기 전에** 부른다.
//
// 자동 관통은 한 번에 한 편치가 나가고 중간에 사람이 보고 있지 않다 — 잔액이 모자란 채
// 시작하면 돈만 나가고 영상이 없다. 그래서 need = 한 편치를 요구한다.
// 단계별은 컷마다 끊겨도 사장님이 화면에서 보고 있으므로 need 를 작게 잡는다.
export async function assertCanStart(userId, { need }) {
  const balance = await balanceFor(userId);
  if (balance < need) throw new NoCredits(balance);
}
```

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run tests/credits.test.js` / Expected: PASS
- [ ] **Step 5: Commit** — `git add lib/credits.js tests/credits.test.js && git commit -m "feat: 잔액은 두 합계의 차 — 편수는 내림으로 보여준다"`

---

### Task 3: 호출 게이트 — `limitUser()` 를 잔액으로

**Files:**
- Modify: `lib/costs.js:115-119` (`limitUser`), `lib/costs.js:134-152` (`assertBudget` 의 사용자 축)
- Test: `tests/credits-gate.test.js` (신규, describe "호출 게이트")

**Interfaces:**
- Consumes: `balanceFor(userId)` (Task 2)
- Produces: `assertBudget` 의 사용자 축이 **잔액**으로 판정한다. 시그니처·`BudgetExceeded` 는 그대로(`scope: "user"` 유지) — 호출부 셋(`i2v`·`imagegen`·`tts`)은 무수정

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/credits-gate.test.js`:

```js
// 호출 게이트 — fal 로 나가기 직전. 잔액을 넘으면 요청 자체가 안 나가야 한다.
// 옛 quick-create-budget.test.js 가 지키던 자리다(t2v 와 함께 삭제됐다).
import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";
import { resetMemoryStore } from "../lib/store/memory.js";
import { getStore } from "../lib/store/index.js";
import { assertBudget } from "../lib/costs.js";
import { runWithActor } from "../lib/actor.js";

const A = "00000000-0000-4000-8000-00000000000a";
const ADMIN = "00000000-0000-4000-8000-0000000000ad";
const ORIG = { ...process.env };

describe("호출 게이트 — 사용자 축은 잔액이다", () => {
  beforeEach(() => {
    resetMemoryStore();
    // 전역·프로젝트 상한은 이 테스트의 관심사가 아니다 — 넉넉히 열어 둔다
    process.env.SHOTFORM_BUDGET_TOTAL_USD = "1000";
    process.env.SHOTFORM_BUDGET_PROJECT_USD = "1000";
    delete process.env.SHOTFORM_FAKE;
    delete process.env.SHOTFORM_FAKE_IMAGES;
  });
  afterEach(() => {
    process.env.SHOTFORM_BUDGET_TOTAL_USD = ORIG.SHOTFORM_BUDGET_TOTAL_USD;
    process.env.SHOTFORM_BUDGET_PROJECT_USD = ORIG.SHOTFORM_BUDGET_PROJECT_USD;
    process.env.SHOTFORM_FAKE = ORIG.SHOTFORM_FAKE;
  });

  it("충전이 없으면 첫 호출부터 막는다 — 옛 고정 상한($5)이 남아 있으면 통과해 버린다", async () => {
    await runWithActor(A, async () => {
      await expect(assertBudget({ endpoint: "fal-ai/kling-video/v3/standard/image-to-video", amount: 5 }))
        .rejects.toMatchObject({ name: "BudgetExceeded", scope: "user" });
    });
  });

  it("충전이 있으면 그 안에서 통과한다", async () => {
    await getStore().insertGrant({ user_id: A, amount_usd: 10, reason: "충전", granted_by: ADMIN });
    await runWithActor(A, async () => {
      await expect(assertBudget({ endpoint: "fal-ai/kling-video/v3/standard/image-to-video", amount: 5 }))
        .resolves.toBeUndefined();
    });
  });

  it("쓴 만큼 줄어들어 결국 막힌다", async () => {
    const store = getStore();
    await store.insertGrant({ user_id: A, amount_usd: 1, reason: "충전", granted_by: ADMIN });
    await store.addRecord({
      request_id: "spent-1", ts: 1, endpoint: "fal-ai/x", stage: "영상",
      actor: A, est_cost_usd: 0.95, status: "done",
    });
    await runWithActor(A, async () => {
      await expect(assertBudget({ endpoint: "fal-ai/kling-video/v3/standard/image-to-video", amount: 5 }))
        .rejects.toMatchObject({ name: "BudgetExceeded", scope: "user" });
    });
  });

  it("전역 상한은 그대로 회사 안전핀이다 — 잔액이 넉넉해도 막힌다", async () => {
    process.env.SHOTFORM_BUDGET_TOTAL_USD = "0.01";
    await getStore().insertGrant({ user_id: A, amount_usd: 1000, reason: "충전", granted_by: ADMIN });
    await runWithActor(A, async () => {
      await expect(assertBudget({ endpoint: "fal-ai/kling-video/v3/standard/image-to-video", amount: 5 }))
        .rejects.toMatchObject({ name: "BudgetExceeded", scope: "total" });
    });
  });
});
```

⚠️ `runWithActor` 의 실제 export 이름·시그니처는 `lib/actor.js` 를 열어 확인하고 맞춰라. 엔드포인트 문자열은 `lib/costs.js` 의 `PRICE_TABLE` 에 실제로 있는 prefix 를 써라(없으면 기본 단가로 떨어져 금액이 달라진다).

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run tests/credits-gate.test.js` / Expected: FAIL — 첫 테스트가 통과해 버린다(고정 $5 상한이 살아 있어서)

- [ ] **Step 3: 구현** — `lib/costs.js` 의 `limitUser` 를 지우고, 사용자 축을 잔액으로 바꾼다:

```js
// (파일 상단 import 에 추가)
import { balanceFor } from "./credits.js";
```

`assertBudget` 안의 사용자 축(현재 144~146행)을 교체:

```js
  // 사용자 축은 이제 **잔액**이다(고정 상한이 아니라). 충전 − 쓴 것 = 남은 것이고,
  // 이번 호출의 예상 비용이 그것을 넘으면 나가기 전에 막는다.
  //
  // ⚠️ 병렬 호출은 같은 잔액을 읽는다 — 자동 관통이 컷마다 동시에 부르므로 최대
  // 한 컷 값만큼 넘길 수 있다. 시작 게이트가 한 편치를 이미 확인했으므로 초과는
  // 소액이고, 음수가 되면 다음 시작 게이트가 막는다. 행 락으로 완전히 막으면 관통이
  // 직렬화돼 컷 12개에 3분 24초가 된다(저장 계층에서 실측한 트레이드오프).
  const actor = costActor();
  const balance = await balanceFor(actor);
  if (cost > balance) throw new BudgetExceeded(0, round6(balance), "user");
```

⚠️ `BudgetExceeded(spent, limit, scope)` 의 메시지가 `예산 상한($limit)에 닿아 멈췄어요 — 지금까지 $spent 썼어요` 이므로, 사용자 축의 문구가 어색해진다. **메시지를 scope 별로 가른다** — `BudgetExceeded` 생성자를 다음으로 바꾼다(전역·프로젝트 문구는 글자 그대로 유지):

```js
  constructor(spent, limit, scope) {
    super(
      scope === "user"
        ? `크레딧이 모자라요 — 남은 크레딧이 $${Number(limit).toFixed(2)}예요`
        : `예산 상한($${limit})에 닿아 멈췄어요 — 지금까지 $${spent.toFixed(2)} 썼어요`
    );
    this.name = "BudgetExceeded";
    this.scope = scope; // "total" | "user" | "project"
  }
```

`round6` 이 파일 안에 이미 있는지 확인하고, 없으면 `Number(balance.toFixed(6))` 로 쓴다.

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run tests/credits-gate.test.js tests/budget.test.js tests/budget-user.test.js tests/costs.test.js` / Expected: PASS. `budget-user.test.js` 는 고정 상한을 뜻하고 있을 가능성이 높다 — **깨지면 그 테스트가 낡은 것**이니 잔액 기준으로 고쳐 쓰고, 무엇을 왜 바꿨는지 보고서에 남겨라(테스트를 약화시키지는 마라)
- [ ] **Step 5: Commit** — `git add lib/costs.js tests/credits-gate.test.js tests/budget-user.test.js && git commit -m "feat: 사용자 상한을 잔액으로 — 전역·프로젝트는 안전핀 그대로"`

---

### Task 4: 시작 게이트 — 자동 관통과 단계별

**Files:**
- Modify: `app/api/projects/[id]/auto/route.js` (멱등 가드 앞)
- Modify: `app/api/projects/[id]/voice/route.js` · `images/route.js` · `clips/route.js`
- Modify: `app/api/projects/[id]/cuts/[idx]/regen/route.js` · `voice/[idx]/regen/route.js` · `clips/[idx]/regen/route.js`
- Test: `tests/credits-gate.test.js` (describe "시작 게이트" 추가)

**Interfaces:**
- Consumes: `assertCanStart(userId, { need })` · `NoCredits` · `perVideoUsd()` (Task 2)
- Produces: 잔액 부족 시 **402** `{ error: "크레딧이 모자라요 — 운영자에게 문의해 주세요" }`. 자동 관통은 `need = perVideoUsd()`, 나머지는 `need = 0.01`(=잔액이 사실상 0 이면 막는다)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/credits-gate.test.js` 에 추가:

```js
import { POST as autoPOST } from "../app/api/projects/[id]/auto/route.js";
import { USER_HEADER, STATUS_HEADER, ROLE_HEADER } from "../lib/auth/headers.js";
import * as projects from "../lib/projects.js";
vi.mock("../lib/auto.js", () => ({ runAutoPipeline: vi.fn(async () => {}) }));
import { runAutoPipeline } from "../lib/auto.js";

const headersFor = (id) => ({
  [USER_HEADER]: id, [STATUS_HEADER]: "approved", [ROLE_HEADER]: "user",
  "content-type": "application/json",
});
const autoReq = (id) =>
  new Request("http://localhost/api/projects/x/auto", {
    method: "POST", headers: headersFor(id), body: JSON.stringify({ voice_label: "차분한 여성" }),
  });
const ctx = (id) => ({ params: Promise.resolve({ id }) });

async function makeProject(ownerId = A) {
  return projects.createProject({
    ownerId, settings: { aspect_ratio: "9:16", target_seconds: 30 },
    material: { text: "자료", photos: [] },
  });
}

describe("시작 게이트 — 자동 관통", () => {
  beforeEach(() => { resetMemoryStore(); vi.clearAllMocks(); delete process.env.SHOTFORM_FAKE; });

  it("잔액이 없으면 402 이고 파이프라인이 안 불린다", async () => {
    const p = await makeProject();
    const res = await autoPOST(autoReq(A), ctx(p.id));
    expect(res.status).toBe(402);
    expect(runAutoPipeline).not.toHaveBeenCalled();
  });

  it("한 편치가 모자라면 402 — 반 편만 있는 상태로 시작시키지 않는다", async () => {
    const p = await makeProject();
    await getStore().insertGrant({ user_id: A, amount_usd: 1.0, reason: "충전", granted_by: ADMIN });
    expect((await autoPOST(autoReq(A), ctx(p.id))).status).toBe(402);
    expect(runAutoPipeline).not.toHaveBeenCalled();
  });

  it("한 편치가 있으면 202 로 시작한다", async () => {
    const p = await makeProject();
    await getStore().insertGrant({ user_id: A, amount_usd: 10, reason: "충전", granted_by: ADMIN });
    expect((await autoPOST(autoReq(A), ctx(p.id))).status).toBe(202);
    expect(runAutoPipeline).toHaveBeenCalledTimes(1);
  });

  it("가짜 모드에서는 막지 않는다 — 0원이라 잴 것이 없다", async () => {
    process.env.SHOTFORM_FAKE = "all";
    const p = await makeProject();
    expect((await autoPOST(autoReq(A), ctx(p.id))).status).toBe(202);
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run tests/credits-gate.test.js` / Expected: FAIL — 402 대신 202

- [ ] **Step 3: 구현** — `app/api/projects/[id]/auto/route.js` 의 404/400 판정 **뒤**, `updateProject` 앞에 추가:

```js
import { assertCanStart, NoCredits, perVideoUsd } from "../../../../../lib/credits";
import { fakeFal } from "../../../../../lib/fake";
```

```js
  // 시작 게이트 — 한 편치가 없으면 시작 자체를 막는다.
  // 자동 관통은 한 번에 ~한 편치가 백그라운드로 나가고 중간에 사람이 보고 있지 않다.
  // 잔액이 모자란 채 시작하면 호출 게이트가 중간에 끊고, 사장님에게는
  // "돈은 나갔는데 영상이 없다"만 남는다.
  //
  // 가짜 모드는 건너뛴다 — 0원이라 잴 것이 없고, 크레딧 없이도 흐름을 볼 수 있어야 한다
  // (assertBudget 이 fakeFal() 에서 즉시 통과하는 것과 같은 규칙).
  if (!fakeFal()) {
    try {
      await assertCanStart(user.id, { need: perVideoUsd() });
    } catch (e) {
      if (e instanceof NoCredits) return Response.json({ error: e.message }, { status: 402 });
      throw e;
    }
  }
```

⚠️ **멱등 가드(patchFn 안)보다 앞에 둔다** — 크레딧이 없으면 `auto.state="running"` 을 세우지 않아야 한다. 세워 두면 그 뒤 402 를 받은 사용자가 충전 후 다시 눌러도 "이미 만드는 중"에 막힌다.

나머지 6개 라우트(`/voice`·`/images`·`/clips`·regen 3종)에는 같은 블록을 `need: 0.01` 로 넣는다. import 경로 깊이는 각 파일 위치에 맞춘다(regen 은 한 단계 더 깊다).

```js
      await assertCanStart(user.id, { need: 0.01 });
```

각 라우트의 기존 가드(404·400·409)는 그대로 두고, 크레딧 게이트는 **그 뒤·유료 시작 앞**에 둔다.

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run tests/credits-gate.test.js tests/auto-route.test.js tests/routes.test.js tests/routes-auth.test.js` / Expected: PASS. `auto-route.test.js` 의 기존 202 케이스들이 크레딧 0 때문에 402 로 깨질 것이다 — **각 테스트에 충전 한 줄을 넣어** 고친다(가드를 약화시키지 마라)
- [ ] **Step 5: Commit** — `git add "app/api/projects/[id]" tests/credits-gate.test.js tests/auto-route.test.js && git commit -m "feat: 시작 게이트 — 한 편치 없이 자동 관통을 시작하지 않는다"`

---

### Task 5: 운영자 충전 라우트와 내 잔액 라우트

**Files:**
- Create: `app/api/admin/users/[id]/credits/route.js`
- Create: `app/api/credits/route.js`
- Test: `tests/credits-admin.test.js` (신규)

**Interfaces:**
- Consumes: `getStore().insertGrant(...)` (Task 1) · `balanceFor` · `videosLeft` · `perVideoUsd` (Task 2) · `withUser` (기존)
- Produces:
  - `POST /api/admin/users/[id]/credits` body `{ videos: number, reason: string }` → `200 { balance_usd, videos_left }`. 운영자 전용. `videos` 는 정수(음수 허용, 0 금지), `reason` 은 비어 있으면 400
  - `GET /api/credits` → `200 { balance_usd, videos_left, per_video_usd }` (본인 것)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/credits-admin.test.js`:

```js
import { describe, it, expect, beforeEach } from "vitest";
import { resetMemoryStore } from "../lib/store/memory.js";
import { getStore } from "../lib/store/index.js";
import { USER_HEADER, STATUS_HEADER, ROLE_HEADER } from "../lib/auth/headers.js";
import { POST as grantPOST } from "../app/api/admin/users/[id]/credits/route.js";
import { GET as creditsGET } from "../app/api/credits/route.js";

const A = "00000000-0000-4000-8000-00000000000a";
const ADMIN = "00000000-0000-4000-8000-0000000000ad";
const headersFor = (id, role = "user") => ({
  [USER_HEADER]: id, [STATUS_HEADER]: "approved", [ROLE_HEADER]: role,
  "content-type": "application/json",
});
const grantReq = (who, role, body) =>
  new Request("http://localhost/api/admin/users/x/credits", {
    method: "POST", headers: headersFor(who, role), body: JSON.stringify(body),
  });
const ctx = (id) => ({ params: Promise.resolve({ id }) });

describe("POST /api/admin/users/[id]/credits", () => {
  beforeEach(() => resetMemoryStore());

  it("운영자가 편수로 넣으면 장부에 남고 잔액이 오른다", async () => {
    const res = await grantPOST(grantReq(ADMIN, "admin", { videos: 2, reason: "체험" }), ctx(A));
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.videos_left).toBe(2);
    expect(body.balance_usd).toBeGreaterThan(0);
  });

  it("음수로 회수할 수 있다", async () => {
    await grantPOST(grantReq(ADMIN, "admin", { videos: 3, reason: "충전" }), ctx(A));
    const res = await grantPOST(grantReq(ADMIN, "admin", { videos: -2, reason: "정정" }), ctx(A));
    expect((await res.json()).videos_left).toBe(1);
  });

  it("누가 왜 넣었는지가 남는다 — 감사", async () => {
    await grantPOST(grantReq(ADMIN, "admin", { videos: 1, reason: "체험 1편" }), ctx(A));
    const sum = await getStore().sumGrants(A);
    expect(sum).toBeGreaterThan(0);
    const m = await getStore().listGrantsFor([A]);
    expect(m.get(A)).toBeCloseTo(sum, 6);
  });

  it("운영자가 아니면 403", async () => {
    expect((await grantPOST(grantReq(A, "user", { videos: 1, reason: "내가 나에게" }), ctx(A))).status).toBe(403);
  });

  it("사유가 없으면 400 — 감사 로그가 비면 장부가 아니다", async () => {
    expect((await grantPOST(grantReq(ADMIN, "admin", { videos: 1, reason: "  " }), ctx(A))).status).toBe(400);
  });

  it("0 편은 400", async () => {
    expect((await grantPOST(grantReq(ADMIN, "admin", { videos: 0, reason: "무의미" }), ctx(A))).status).toBe(400);
  });
});

describe("GET /api/credits", () => {
  beforeEach(() => resetMemoryStore());

  it("내 잔액을 편수와 함께 준다", async () => {
    await grantPOST(grantReq(ADMIN, "admin", { videos: 2, reason: "충전" }), ctx(A));
    const res = await creditsGET(new Request("http://localhost/api/credits", { headers: headersFor(A) }), {});
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.videos_left).toBe(2);
    expect(body.per_video_usd).toBeGreaterThan(0);
  });

  it("충전이 없으면 0편", async () => {
    const res = await creditsGET(new Request("http://localhost/api/credits", { headers: headersFor(A) }), {});
    expect((await res.json()).videos_left).toBe(0);
  });
});
```

⚠️ `withUser` 가 핸들러에 넘기는 인자 순서는 `(req, ctx, user)` 다(`lib/auth/require-user.js` 확인). `GET` 은 ctx 를 안 쓰므로 위 테스트가 `{}` 를 넘긴다.

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run tests/credits-admin.test.js` / Expected: FAIL — 두 라우트 파일 없음

- [ ] **Step 3: 구현** — `app/api/admin/users/[id]/credits/route.js`:

```js
// POST /api/admin/users/[id]/credits — 운영자 수동 충전(회수는 음수).
// 결제가 붙기 전까지 크레딧이 들어오는 유일한 문이다.
import { getStore } from "../../../../../../lib/store/index.js";
import { withUser } from "../../../../../../lib/auth/require-user.js";
import { balanceFor, videosLeft, perVideoUsd } from "../../../../../../lib/credits.js";

export const POST = withUser(async (req, { params }, user) => {
  const { id } = await params;
  const body = await req.json().catch(() => ({}));

  // 편수로 받는다 — 운영자도 사장님과 같은 말을 쓰게. 저장은 USD 다(원장과 같은 단위).
  const videos = Number(body?.videos);
  if (!Number.isInteger(videos) || videos === 0) {
    return Response.json({ error: "편수는 0 이 아닌 정수여야 해요" }, { status: 400 });
  }
  const reason = typeof body?.reason === "string" ? body.reason.trim() : "";
  if (!reason) {
    return Response.json({ error: "사유를 적어 주세요" }, { status: 400 });
  }

  await getStore().insertGrant({
    user_id: id,
    amount_usd: videos * perVideoUsd(),
    reason,
    granted_by: user.id,
  });

  const balance = await balanceFor(id);
  return Response.json({ balance_usd: balance, videos_left: videosLeft(balance) });
}, { adminOnly: true });
```

`app/api/credits/route.js`:

```js
// GET /api/credits — 내 잔액. 사이드바가 5분마다도 아니고 화면 진입 때 한 번 읽는다.
import { withUser } from "../../../lib/auth/require-user.js";
import { balanceFor, videosLeft, perVideoUsd } from "../../../lib/credits.js";

export const GET = withUser(async (_req, _ctx, user) => {
  const balance = await balanceFor(user.id);
  return Response.json({
    balance_usd: balance,
    videos_left: videosLeft(balance),
    per_video_usd: perVideoUsd(),
  });
});
```

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run tests/credits-admin.test.js tests/routes-auth.test.js` / Expected: PASS. `routes-auth.test.js` 가 라우트를 열거식으로 검사하면 새 둘을 기존 패턴대로 추가
- [ ] **Step 5: Commit** — `git add app/api/admin/users app/api/credits tests/credits-admin.test.js tests/routes-auth.test.js && git commit -m "feat: 운영자 충전과 내 잔액 — 편수로 받고 USD로 적는다"`

---

### Task 6: 화면 — 사이드바·빠른 생성·백오피스

**Files:**
- Modify: `components/Sidebar.jsx:107-112` (`.credit-box`)
- Modify: `components/QuickCreate.jsx` (요약 카드의 [영상 만들기])
- Modify: `app/admin/page.js` (잔액 열 + 넣기)
- Test: `tests/credits-ui.test.js` (신규, 소스 판정 — 이 저장소에 React 렌더 테스트가 없다)

**Interfaces:**
- Consumes: `GET /api/credits` → `{ balance_usd, videos_left, per_video_usd }` · `POST /api/admin/users/[id]/credits` body `{ videos, reason }` (Task 5)
- Produces: 화면 배선만 — 다른 태스크가 의존하지 않는다

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/credits-ui.test.js`:

```js
// 화면 배선을 소스에서 판정한다(staleness-ui.test.js·quick-create-ui.test.js 패턴).
import { describe, it, expect } from "vitest";
import { readFileSync } from "fs";

const sidebar = readFileSync("components/Sidebar.jsx", "utf8");
const quick = readFileSync("components/QuickCreate.jsx", "utf8");
const admin = readFileSync("app/admin/page.js", "utf8");

describe("사이드바 — 크레딧", () => {
  it("무제한이라고 말하지 않는다", () => {
    expect(sidebar).not.toMatch(/무제한/);
    expect(sidebar).not.toMatch(/크레딧을 차감하지 않아요/);
  });
  it("잔액을 서버에서 읽는다", () => {
    expect(sidebar).toMatch(/\/api\/credits/);
  });
});

describe("빠른 생성 — 잔액 부족", () => {
  it("잔액을 읽고 부족하면 만들기를 막는다", () => {
    expect(quick).toMatch(/\/api\/credits/);
    expect(quick).toMatch(/크레딧이 모자라/);
  });
});

describe("백오피스 — 충전", () => {
  it("충전 라우트를 부른다", () => {
    expect(admin).toMatch(/\/credits/);
  });
  it("사유를 함께 보낸다 — 감사 로그가 비지 않게", () => {
    expect(admin).toMatch(/reason/);
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run tests/credits-ui.test.js` / Expected: FAIL — 사이드바에 "무제한"이 남아 있다

- [ ] **Step 3: 구현** —

`components/Sidebar.jsx`: 파일 상단에 `useEffect`·`useState` 가 이미 import 돼 있는지 확인하고(없으면 추가), 컴포넌트 안에 추가:

```jsx
  // 크레딧 잔액 — 화면 진입 때 한 번 읽는다. 실패하면 조용히 숨긴다(사이드바가
  // 오류로 시끄러워질 자리가 아니다). 소비 뒤 갱신은 새로고침이 맡는다.
  const [credits, setCredits] = useState(null);
  useEffect(() => {
    let alive = true;
    fetch("/api/credits")
      .then((r) => (r.ok ? r.json() : null))
      .then((d) => { if (alive && d) setCredits(d); })
      .catch(() => {});
    return () => { alive = false; };
  }, []);
```

`.credit-box` 를 교체:

```jsx
      <div className="credit-box">
        크레딧
        <b>{credits ? `${credits.videos_left}편 남음` : "…"}</b>
        <small>
          {credits && credits.videos_left === 0
            ? "운영자에게 문의해 주세요 (실비용은 비용 기록에서)"
            : "영상 한 편을 만들 때마다 줄어요 (실비용은 비용 기록에서)"}
        </small>
      </div>
```

`components/QuickCreate.jsx`: 같은 방식으로 `credits` 를 읽고, 요약 카드의 버튼을 막는다:

```jsx
  const noCredits = credits && credits.videos_left < 1;
```

```jsx
                  {m.confirm && (
                    <div className="res-ops">
                      <button
                        className="mini confirm-btn"
                        onClick={() => confirmGenerate(i)}
                        disabled={busy || noCredits}
                      >
                        🎬 영상 만들기
                      </button>
                      {noCredits && (
                        <small>크레딧이 모자라요 — 운영자에게 문의해 주세요</small>
                      )}
                    </div>
                  )}
```

⚠️ `startAuto` 의 catch 는 402 도 오류 문구로 보여준다 — 그대로 두면 된다(서버 메시지가 이미 한국어다).

`app/admin/page.js`: 사용자 목록을 그릴 때 잔액을 함께 보여주고 넣기 버튼을 단다. 목록 로드 뒤 각 사용자의 잔액이 필요하므로 `GET /api/admin/users` 응답에 잔액을 실어 보내는 것이 낫다 — `app/api/admin/users/route.js` 를 다음으로 바꾼다:

```js
// GET /api/admin/users — 승인 대기·전체 사용자 목록 (운영자 전용)
import { withUser } from "../../../../lib/auth/require-user.js";
import { getStore } from "../../../../lib/store/index.js";
import { videosLeft } from "../../../../lib/credits.js";

export const GET = withUser(async () => {
  const store = getStore();
  const users = await store.listProfiles();
  // 잔액은 두 합계의 차다. 행마다 왕복하지 않으려고 충전은 한 번에 받고,
  // 쓴 것은 사용자별로 물어본다(원장 쪽에는 묶음 조회가 아직 없다).
  const grants = await store.listGrantsFor(users.map((u) => u.id));
  const withCredits = await Promise.all(
    users.map(async (u) => {
      const balance = (grants.get(u.id) || 0) - (await store.sumCosts({ actor: u.id }));
      return { ...u, balance_usd: balance, videos_left: videosLeft(balance) };
    })
  );
  return Response.json({ users: withCredits });
}, { adminOnly: true });
```

`app/admin/page.js` 의 각 사용자 행에 잔액과 버튼을 넣는다(기존 승인·차단 버튼 옆):

```jsx
                    <span className="st-badge">{u.videos_left ?? 0}편</span>
                    <button className="mini" disabled={busy === u.id} onClick={() => grant(u.id)}>
                      크레딧 넣기
                    </button>
```

```jsx
  // 편수와 사유를 받아 넣는다. prompt 를 쓰는 이유는 운영자 전용 화면이고 이 동작이
  // 드물기 때문이다 — 전용 모달을 만들 만큼의 빈도가 아니다(필요해지면 그때 만든다).
  async function grant(id) {
    const raw = window.prompt("몇 편을 넣을까요? (회수는 음수)", "1");
    if (raw === null) return;
    const videos = Number(raw);
    if (!Number.isInteger(videos) || videos === 0) return;
    const reason = window.prompt("사유를 적어 주세요", "체험");
    if (!reason || !reason.trim()) return;
    setBusy(id);
    try {
      const r = await fetch(`/api/admin/users/${id}/credits`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ videos, reason }),
      });
      if (!r.ok) throw new Error((await r.json()).error || "충전 실패");
      await load();
    } catch (e) {
      setErr(e.message);
    } finally {
      setBusy(null);
    }
  }
```

⚠️ `load`·`setBusy`·`setErr` 의 실제 이름은 `app/admin/page.js` 를 열어 맞춰라. `window.prompt` 는 브라우저 모달이라 자동화 도구를 막지만 **운영자 전용 화면이라 허용**한다 — 다만 그 사실을 주석에 남겨라.

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run tests/credits-ui.test.js tests/design-system.test.js tests/admin-and-labels.test.js` / Expected: PASS
- [ ] **Step 5: Commit** — `git add components/Sidebar.jsx components/QuickCreate.jsx app/admin/page.js app/api/admin/users/route.js tests/credits-ui.test.js && git commit -m "feat: 화면이 잔액을 말한다 — 무제한 자리를 편수로"`

---

### Task 7: 전체 그린 + 가짜 모드 관통 검증

**Files:** 없음 (검증 태스크 — 결함이 나오면 고치지 말고 보고)

**Interfaces:**
- Consumes: Task 1~6 전부
- Produces: 관통 증거, 발견 결함 목록

- [ ] **Step 1: 전체 스위트** — Run: `npx vitest run` / Expected: 전체 PASS, 새 실패 0. 깨진 것이 있으면 **낡은 테스트인지 진짜 회귀인지 가려** 보고하라
- [ ] **Step 2: 스키마 적용 확인** — `db/schema.sql` 을 Supabase SQL 편집기에 **통째로** 다시 올려도 안전해야 한다(`if not exists`·`or replace`). 실제로 올릴지는 사용자에게 물어라 — 라이브 DB 를 건드리는 일이다
- [ ] **Step 3: 가짜 모드 관통** — 3000 포트는 사용자 소유이므로 건드리지 말고, `next.config.mjs` 에 임시 `distDir` env 스위치를 넣어 다른 빌드 디렉터리로 띄운다(끝나면 원복·커밋 금지):

```bash
PORT=3001 SHOTFORM_FAKE=all SHOTFORM_DIST_DIR=.next-verify npm run dev
```

확인할 것: ① `GET /api/credits` 가 0편을 준다 ② 사이드바가 "0편 남음"을 보여준다 ③ 가짜 모드라 [영상 만들기]는 **막히지 않는다**(0원이므로 — 시작 게이트가 `fakeFal()` 을 건너뛴다) ④ `POST /api/admin/users/<id>/credits` 로 2편을 넣으면 잔액이 오른다

- [ ] **Step 4: 실모드 게이트 확인(0원)** — `SHOTFORM_FAKE` 없이 띄우되 **생성 버튼을 누르지 않는다.** `curl` 로 `POST /api/projects/<id>/auto` 만 쳐서 **402** 와 그 뒤 충전 후 202 를 확인한다(202 는 파이프라인을 실제로 시작하므로 **하지 않는다** — 402 만 확인하고, 202 확인은 가짜 모드로 대신한다)
- [ ] **Step 5: 결과 보고** — 관통 로그·잡은 결함을 보고. **fal 라이브 검증은 사용자 승인 후**

---

## Self-Review 결과 (계획 작성 시 수행)

- **스펙 커버리지**: 스키마·`sum_grants`(T1) · 잔액/편수/시작 게이트 판정(T2) · 호출 게이트(T3) · 시작 게이트 배선 7곳(T4) · 운영자 충전·내 잔액(T5) · 사이드바·빠른 생성·백오피스(T6) · 테스트 항목 6종(T1~T6에 분산) · 관통(T7). **누락 없음.**
- **타입 일치**: `sumGrants(userId)→number` · `insertGrant({user_id,amount_usd,reason,granted_by})` · `listGrantsFor(ids)→Map` 를 T1 정의, T2·T5·T6 가 같은 이름으로 소비. `balanceFor`·`videosLeft`·`perVideoUsd`·`assertCanStart`·`NoCredits` 를 T2 정의, T3·T4·T5 가 사용. 라우트 응답 `{balance_usd, videos_left, per_video_usd}` 를 T5 정의, T6 소비.
- **주의로 남긴 것**: `runWithActor`·`addRecord` 픽스처·`load/setBusy/setErr` 이름은 실물 대조 지시를 각 태스크에 명시했다(픽스처를 실물에 맞추고 구현을 고치지 않는 규칙 포함).
