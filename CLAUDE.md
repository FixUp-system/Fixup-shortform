# shotform-saas — 작업 지침

## 세션 마무리 규칙

**세션을 마칠 때는 항상 지금까지의 작업을 정리해 wiki(`C:\Users\fixup\obsidian_jaechan`)에
반영한다.** 사용자가 따로 요청하지 않아도 한다.

- 보관함의 `CLAUDE.md`(운영 지침)와 `index.md`를 먼저 읽고 그 규약을 따른다
- 관련 페이지(`products/shotform-saas`, `concepts/shotform-*`)를 갱신하고,
  새로 배운 것이 크면 `sources/`에 소스 페이지를 만든다
- `index.md`·`log.md`도 함께 갱신한다
- **wiki 파일은 Write/Edit 도구로만 쓴다.** PowerShell로 쓰면 한글이 깨진다
  (Windows PowerShell 5.1의 인코딩 기본값 문제. 실제로 한 번 파일을 깨뜨렸다)

## 이 저장소에서 일하는 방식

- **측정 없이 품질을 주장하지 않는다.** 프롬프트를 고쳤으면 실제로 돌려서 수치를 본다.
  → 스킬 `measuring-llm-prompt-changes`(`.claude/skills/`)에 방법이 정리돼 있다
- 지켜져야 하는 것(길이·원문 보존·중복)은 프롬프트가 아니라 **코드가 판정**한다
- 임계값은 감이 아니라 **실측 분포**에서 뽑는다
- 프롬프트 예시는 테스트에 쓸 자료와 **소재도 동사도** 겹치지 않게 고른다

## 실행

```bash
npm run dev                      # localhost:3000
SHOTFORM_FAKE_IMAGES=1 npm run dev   # 이미지 무료(인라인 SVG) — 흐름만 볼 때
npx vitest run                   # 테스트
```

> ⚠️ **dev 서버를 켜둔 채 `npm run build`를 돌리지 마라.** `.next`가 덮여 dev 서버가 죽는다.
> 죽었으면 서버를 끄고 `.next`를 지운 뒤 다시 띄운다.

측정:
```bash
node scripts/measure/run-pipeline.mjs tailor 3 30   # 자료·반복·목표초
node scripts/measure/run-pipeline.mjs thin 1 --cuts # 컷·화면까지(가짜 이미지 모드 권장)
node scripts/measure/ab-briefing.mjs setup|b        # 프롬프트 A/B
```
서버 로그에 `[대본 xxxxxxxx]`로 되돌리기 라운드별 글자 수·결함·점수·채택 여부가 남는다.

## 지금 구조 (2026-07-27 밤)

```
자료+길이 선택 → 브리핑(질문 있을 때만 멈춤) → 원고(승인 게이트) → 컷 분할 → 화면 설계 → 이미지
```

- **원고가 원본이다.** 컷은 원고를 잘라서 만든다 — LLM은 경계 번호만 고르고 코드가 자른다.
  `컷을 이어붙이면 원고와 글자 그대로 같다`가 이 파이프라인의 유일한 구조적 보장이다
- 목표 길이 = 사장님이 고른 초 × 5.5자, 안 골랐으면 자료의 사실 수 × 30자(60~220)
- 대본 라우트는 **초안 → 되돌리기(최대 3회) → 교정**. 되돌리기 채택은 `scriptScore`
  (결함 수 × 1000 + 목표와의 거리)가 낮아질 때만
- 구성(synopsis) 단계는 폐지됐다. 옛 프로젝트 폴백만 남아 있다

## 이어서 할 일

1. **`overTarget` 임계를 좁힌다** — 지금 목표의 1.3배(30초 요청에 39초까지 정상 취급).
   1.15배로 좁히고, 되돌리기 지시에 "목표 ±10% 안으로, 그 아래로 깎지 마라"(진자 방지)를 넣는다.
   근거: 로그상 202자·205자짜리가 결함 없음으로 통과했다
2. **약한 오프닝 되돌리기가 안 먹는다** — 첫 문장만 바꾸라고 지목해도 원고 통째 재작성에 묻힌다.
   첫 문장만 받아 갈아 끼우는 방식이 맞다
3. **브리핑 질문의 값어치** — "가게 이름·전화번호"처럼 낭독에 쓰지 않을 것을 묻는다.
   "이 답이 낭독 문장에 들어갈 수 있는가" 필터가 필요하다
4. **컷이 문장보다 잘아질 수 없다**(12~13초 컷) — 쉼표·연결어미까지 분할 후보를 넓히거나
   컷 하나에 이미지 여러 장. **영상화(P1)·TTS와 함께 하는 것이 맞다**(지금은 겪지 않는 문제)
5. 얕은 자료의 카피성 표현("새 생명을 얻습니다")
6. 실제 fal 이미지는 미검증 — 지금까지 가짜 이미지 모드로만 돌렸다
