// 목소리 후보 — 화면(클라이언트)과 라우트(서버)가 함께 본다.
//
// lib/tts.js 에 두면 안 된다. 그 모듈은 costs.js 를 거쳐 fs 를 끌고 오는데,
// "use client" 화면이 import 하면 번들에 fs 가 들어가 빌드가 깨진다.
// 목록은 순수 데이터라 여기 따로 둔다.
//
// ⚠️ id 는 fal 문서에서 확인해 채워야 한다. 비어 있으면 실제 호출은 라우트가 막고,
//    가짜 모드에서는 그대로 통과시킨다(흐름 확인이 목적이므로).
export const VOICES = [
  { id: "", label: "차분한 여성" },
  { id: "", label: "밝은 여성" },
  { id: "", label: "차분한 남성" },
  { id: "", label: "밝은 남성" },
];
