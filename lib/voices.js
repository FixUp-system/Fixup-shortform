// 목소리 후보 — 화면(클라이언트)과 라우트(서버)가 함께 본다.
//
// lib/tts.js 에 두면 안 된다. 그 모듈은 costs.js 를 거쳐 fs 를 끌고 오는데,
// "use client" 화면이 import 하면 번들에 fs 가 들어가 빌드가 깨진다.
// 목록은 순수 데이터라 여기 따로 둔다.
//
// id 는 fal-ai/elevenlabs/tts/* 가 받는 음성 "이름"이다(UUID 가 아니다. 기본값은 "Rachel").
// turbo v2.5 는 다국어 모델이라 영어 이름 음성도 한국어를 읽는다.
//
// ⚠️ 이 매핑(라벨 ↔ 음성)은 fal 문서를 보고 고른 것이고 아직 귀로 확인하지 않았다.
//    관통에서 실제 낭독을 듣고 라벨과 어긋나면 여기서 바꾼다. 억양이 어색한 음성도
//    그때 걸러진다 — 한국어 낭독은 음성마다 편차가 크다.
export const VOICES = [
  { id: "Sarah", label: "차분한 여성" },
  { id: "Laura", label: "밝은 여성" },
  { id: "George", label: "차분한 남성" },
  { id: "Eric", label: "밝은 남성" },
];
