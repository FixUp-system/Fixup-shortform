// 사장님이 쓰는 소재 글의 길이 상한 — **한 자리**다.
//
// ★★ 두 벌이었다(2026-08-19): 화면은 maxLength={2000}, 서버는 slice(0, 4000). 화면이 더
//   빡빡해서 사장님은 2,000자에서 막히는데 서버는 4,000자까지 받을 생각이었다. 두 값이
//   갈리면 언제나 **좁은 쪽이 이기고** 넓힌 쪽은 아무 일도 안 한다.
//
// ★ 완전 무제한으로 두지 않는다 — 이 글은 그대로 LLM 프롬프트에 실린다. 실수로 파일을
//   통째로 붙여넣으면 그 호출값이 그대로 나간다. 상한은 "사장님이 실제로 쓸 일이 없는
//   크기"면 충분하다: 20,000자는 한국어로 원고지 100장이 넘는다.
// ★ import 가 없다 — "use client" 화면이 그대로 부른다.
export const MAX_MATERIAL_TEXT = 20000;
