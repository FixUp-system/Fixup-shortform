// 크레딧 가격표 — 이 파일이 유일한 진실의 원천이다.
//
// 값은 2026-08-06 실측 원가(원장 63건, 전부 진짜 지출)에서 뽑았다:
//   클립 $0.420/컷 · 이미지 $0.080/컷 · 나머지(LLM) 편당 ~$0.06
//   → 편당 원가 ≈ $0.06 + 컷당 $0.50 × 컷 수. 30초(6컷) ≈ $3.06
// 1크레딧 ≈ $0.06 원가로 잡아 30초 = 50 크레딧(Kling 기준. 모델마다 갈린다 — VIDEO_PRICE 참고).
// 마진은 없다 —
// 지금 크레딧은 사실상 사용 한도이고, 판매가는 결제를 붙일 때 정한다.
//
// ★ 이 값들은 바뀐다. 바뀔 때 고칠 자리가 여기 하나여야 한다 —
//   가격 숫자를 라우트·화면에 흘리지 마라.
//
// ★ 이 파일은 화면("use client")에서도 import 된다. import 문을 두지 마라 —
//   순수 데이터·순수 함수만 있어야 번들이 깨지지 않는다.

// 목표 길이(초) → 크레딧. **모델마다 다르다** — 원가가 모델의 초당 단가에 비례한다.
//   kling-v3     : 클립 $0.084/s → 30초 한 편 원가 ≈ $3.06
//   seedance-2.0 : 클립 $0.3034/s → 30초 한 편 원가 ≈ $9.64 (3.2배)
// 1크레딧 ≈ $0.06 원가 기준은 그대로다.
//
// ★ 모델 × 해상도 × 길이. 배수 계산을 쓰지 않고 표에 박는다 — 반올림이 생기고
// 모델마다 배율이 다른 것을 못 담는다(아래 AD_VIDEO_PRICE 와 같은 판단).
//
// 원가 근거: **`lib/costs.js` 의 `bytedance/seedance-2.0` 항목이 정본이다** — 원장과 예산
// 가드가 실제로 그 값으로 돈다. 720p $0.3034/s(fal 문서의 t2v 값). i2v 문서값은 $0.3024 로
// 0.3% 낮지만, 접두사가 하나라 두 값을 둘 수 없어 **비싼 쪽으로 통일**했다(적게 잡으면
// 예산 가드가 뚫린다 — costs.js 그 줄의 주석 참고). 여기 근거를 적을 때도 그 값을 쓴다.
// 1080p·480p 는 **계산값**이다 — 원가가 픽셀수(가로폭 제곱)에 비례한다는 관측에서 왔다
// (광고 t2v 실측 0.682/0.3034 = 2.248 vs 1080²/720² = 2.25). 실청구와 어긋나면 이 표를 고친다.
//   1080p = 0.3034 × 2.25 ≈ 0.682/s · 480p = 0.3034 × 4/9 ≈ 0.1348/s
//
// ★★ 원가비와 **크레딧 값**은 다른 규칙이다 — 헷갈리기 쉬우니 갈라서 적는다.
//   · 1080p 는 원가비 2.25 를 **그대로** 썼다(160 × 2.25 = 360, 딱 떨어진다).
//   · 480p 는 원가비가 4/9(≈0.444)지만 그대로 쓰면 36/71/107/142 가 나온다. 이 저장소의
//     가격표는 전부 5·10 단위의 깔끔한 숫자라(25/50/75/100 · 80/160/240/320) 그 관례에
//     맞춰 **5의 배수로 올렸고**, 결과적으로 720p 의 **정확히 1/2**(40/80/120/160)이 됐다.
//     ≈12.5% 초과 청구이고 **올림 방향이라 우리에게 안전하다** — 첫 청구서와 대조할 때
//     값을 내리는 것이 올리는 것보다 싸다.
//   즉 480p 열을 읽고 "4/9 로 계산했구나"라고 읽으면 안 된다. **1/2 이다.**
export const VIDEO_PRICE = {
  // ★★ 2026-08-27 — **1회 생성 기준으로 다시 계산했다**(사장님 지시: "1회 생성 기준으로
  //   갱신해줘 스토리보드 포함해서"). 그전 표는 **클립값만** 담고 있어서, 한 편을 만들 때
  //   함께 나가는 그림·LLM·음성인식이 어디에도 안 실려 있었다 — 15초 720p 는 정가가
  //   원가를 $0.16 밑돌았다(실측: 클립 $4.55 + 스토리보드 $0.401 + 나머지).
  //
  //   한 칸 = **1회 생성 원가 ÷ $0.06, 5크레딧 단위로 올림**. 원가는 셋을 더한다:
  //     ① 클립  = 초 × 모델·해상도 단가 (lib/costs.js 의 PRICE_TABLE 이 정본)
  //     ② 그림  = **max($0.401, $0.08 × 컷 수)** — 흐름이 둘이라 **큰 쪽**을 잡는다.
  //        reel 은 스토리보드 **한 장**($0.401, GPT Image 2 high)이고, 단계별은 컷마다
  //        한 장($0.08)이다. 컷 수는 5초에 하나로 본다 — 30초부터 컷별 쪽이 더 비싸진다.
  //     ③ 나머지 = **$0.30** 한 덩어리 — LLM(실측 $0.13~0.25) + TTS + 음성인식(whisper).
  //        길이에 거의 안 딸리고 각각이 작아서 나눠 적으면 표만 어수선해진다.
  //
  //   ⚠️ **1회 기준이다.** 다시 만들기(그림·영상·시나리오)는 이 표에 안 들어 있고
  //     지금 reel 에는 재생성 청구가 없다 — 그 구멍은 이 표가 아니라 청구 자리에서 막는다.
  //   ★ 값이 맞는지는 주석이 아니라 **tests/pricing-covers-cost.test.js 가 잰다** —
  //     lib/costs.js 에서 원가를 다시 계산해 "정가 ≥ 1회 원가"와 "5크레딧 넘게 더 받지
  //     않는다"를 함께 본다. 단가가 바뀌면 그 판이 먼저 깨진다.
  "seedance-2.0": {
    // 1회 원가 $2.72 · $4.82 · $7.09 · $9.35
    "480p": { 15: 50, 30: 85, 45: 120, 60: 160 },
    // 1회 원가 $5.25 · $9.88 · $14.67 · $19.46
    "720p": { 15: 90, 30: 165, 45: 245, 60: 325 },
    // 1회 원가 $10.93 · $21.24 · $31.71 · $42.18
    "1080p": { 15: 185, 30: 355, 45: 530, 60: 705 },
  },
  // ★★ Seedance 2.5 (2026-08-25 — 사장님이 "등급에 따라서 사용 가능하게" 해서 열렸다).
  // ★ 45·60초는 이 모델이 안 받는다(secondsForModel 이 15·30 만 연다). 그래도 적는 이유는
  //   videoPrice 가 모르는 길이를 table[30] 으로 떨어뜨리는 모양을 지키려는 것이다.
  // ⚠️ **원가가 실측이 아니다** — 2.5 를 한 번도 불러본 적이 없다(lib/ad/models.js 의
  //   perSecUsd 주석). 첫 청구서와 대조해 실제 값이 더 크면 이 표를 올려야 한다.
  "seedance-2.5": {
    // 1회 원가 $3.79 · $6.95 · $10.27 · $13.60
    "480p": { 15: 65, 30: 120, 45: 175, 60: 230 },
    // 1회 원가 $7.63 · $14.65 · $21.82 · $28.99
    "720p": { 15: 130, 30: 245, 45: 365, 60: 485 },
  },
  // 해상도를 안 받는 모델도 같은 모양을 쓴다 — 두 모양이면 videoPrice 에 분기가 생긴다.
  // ★ 이 모델은 **단계별 흐름**의 것이다(컷마다 그림 한 장) — 위 ② 의 "큰 쪽" 규칙이
  //   여기서는 컷별 쪽으로 걸린다. 옛 표(25/50/75/100)는 그 그림값을 덜 잡고 있었다.
  // 1회 원가 $1.96 · $3.30 · $4.80 · $6.30
  "kling-v3": {
    "720p": { 15: 35, 30: 55, 45: 80, 60: 105 },
  },
};

// 화면이 import 하는 파일이라 lib/clip-limits.js 를 끌어오지 않는다(import 문을 늘리면
// 사슬 끝에 fs 가 닿을 수 있다). 값이 갈리는 것은 tests/pricing.test.js 가 막는다.
const PRICE_DEFAULT_RESOLUTION = "720p";

// 컷 하나를 다시 만들 때. 클립만 모델·해상도를 탄다(이미지·목소리는 둘 다 무관하다).
// 실측 원가 이미지 $0.08 · 목소리 $0.002 · 클립 kling $0.42 / seedance $1.51 을 올림했다.
// 해상도 배수는 VIDEO_PRICE 와 같다 — 1080p 는 2.25(25×2.25=56.25 → 57).
// 480p 는 위 표와 달리 **두 규칙이 안 갈린다**: 원가비 4/9 로 25×4/9=11.1, 720p 의 1/2 로
// 25×0.5=12.5 — 올리면 둘 다 12 다. 그래서 여기서는 고를 것이 없었다.
export const REGEN_PRICE = {
  image: 2,
  voice: 1,
  clip: {
    "seedance-2.0": { "480p": 12, "720p": 25, "1080p": 57 },
    // ★ 2.5 는 2.0 의 약 1.52배다(원가비 0.2056/0.1348 · 0.4622/0.3034 — 둘 다 1.52).
    //   올려 잡는다: 12×1.52=18.3→19 · 25×1.52=38.1→39. 이 줄이 없으면 priceModel 이
    //   조용히 kling 값(8)으로 떨어져 컷 하나를 원가의 몇 분의 일에 판다.
    "seedance-2.5": { "480p": 19, "720p": 39 },
    "kling-v3": { "720p": 8 },
  },
};

// ★ 모델을 안 넘긴 호출이 떨어질 자리. **기본 모델이 아니라 옛 모델이다.**
// 모델을 모른다는 것은 이 기능 전에 만들어진 프로젝트라는 뜻이고, 그것들은 Kling 으로 돈다.
// (같은 규칙이 lib/clip-limits.js 의 LEGACY_I2V_MODEL 에 있다. 이 파일은 import 를 둘 수
//  없어 — 화면이 읽는 순수 데이터 파일이다 — 문자열을 되풀이한다. 한쪽을 바꾸면 둘 다 바꿔라.)
const LEGACY_MODEL = "kling-v3";

function priceModel(model) {
  return VIDEO_PRICE[model] ? model : LEGACY_MODEL;
}

// 컷마다 이만큼은 공짜다 — "한 번은 다시 해 볼 수 있게".
export const FREE_REGEN_PER_CUT = 1;

// 컷마다 이 횟수를 넘으면 아예 못 한다(돈을 내도). lib/pipeline.js 의 세 regen 이 락 안에서
// 세는 상한과 **같은 값이어야 한다** — 라우트가 청구 앞에서 먼저 보고 막는다.
// 숫자가 두 군데로 갈리면 "받고 나서 400" 이 다시 생긴다(실제로 그랬다).
export const MAX_REGEN_PER_CUT = 3;

// 백오피스 [크레딧 넣기] 의 기본값(운영자가 고칠 수 있다).
export const DEFAULT_GRANT = 500;

// 운영자가 **처음 승인할 때** 자동으로 들어가는 가입 기본 크레딧.
//
// ★ DEFAULT_GRANT 와 같은 500 이지만 **다른 값이다.** 저것은 운영자가 화면에서 언제든
// 고쳐 쓰는 입력칸의 기본값이고, 이것은 코드가 사람 손 없이 주는 값이다. 하나로 묶으면
// 입력칸 기본값을 바꾸는 순간 자동 지급액이 조용히 따라 움직인다.
//
// ⚠️ 이 숫자는 현금이다 — Seedance 30초 3편(원가 ≈$29) 또는 Kling 10편이다.
export const SIGNUP_GRANT = 500;

// 그 지급의 사유. **멱등키 대신 쓴다** — credit_grants 에는 유니크 제약이 없어서,
// 승인을 껐다 켜면 같은 지급이 또 들어간다. 지급 전에 이 사유의 행이 이미 있는지 본다.
// 문구를 바꾸면 **이미 받은 사람이 한 번 더 받는다** — 바꾸지 마라.
export const SIGNUP_GRANT_REASON = "가입 기본 지급";

// ★ 크레딧은 **개수**다 — 0.7 크레딧으로 살 수 있는 것이 없고, 화면에 "24.4 크레딧
// 차감"이 뜨면 사장님은 그것이 무슨 뜻인지 모른다. 원가에서 값을 다시 뽑다 보면 소수가
// 떨어지는데(원가 $1.47 ÷ $0.06 = 24.5), 그때 **반올림해서** 정수로 내보낸다
// (2026-08-13 사용자 결정).
//
// 표를 정수로 지키는 것만으로는 부족하다 — 원가가 바뀌어 누가 24.4 를 적어 넣는 날 그
// 소수가 그대로 장부에 들어가고 잔액이 소수가 된다. **나가는 자리**에서 막는다.
// 올림도 버림도 아닌 반올림인 이유: 어느 한쪽으로만 기울면 값이 조금씩 어긋난다.
const credits = (n) => Math.round(Number(n) || 0);

// 길이를 모르거나 목록 밖이면 30초 값으로 본다 — 프로젝트의 target_seconds 는
// null 일 수 있고(사장님이 안 고른 경우) 그때 실제로 만들어지는 분량이 그 언저리다.
export function videoPrice(seconds, model, resolution) {
  const byRes = VIDEO_PRICE[priceModel(model)];
  // 모르는 해상도·해상도를 안 받는 모델은 720p 열로 떨어진다. 던지지 않는 이유는
  // 이 함수를 화면이 부르기 때문이다 — 값이 틀리는 것보다 화면이 사라지는 것이 나쁘다.
  const table = byRes[resolution] || byRes[PRICE_DEFAULT_RESOLUTION];
  const p = table[Number(seconds)];
  return credits(typeof p === "number" ? p : table[30]);
}

// 화면에 적는 말. 0 은 "0 크레딧"이 아니라 **무료**다 — 숫자만 적으면 값이 붙은 것처럼
// 읽힌다. 문구를 여기 두는 이유는 세 화면(③목소리·④이미지·⑤영상)이 같은 말을 해야 하기
// 때문이다. 화면마다 적으면 언젠가 한 곳만 바뀐다.
export function priceLabel(credits) {
  return credits > 0 ? `${credits} 크레딧` : "무료";
}

// priorCount = 이 컷에서 이미 한 재생성 횟수. 0 이면 첫 번째라 공짜다.
// 모르는 종류는 던진다 — 오타로 조용히 공짜가 되는 것이 이 표에서 가장 위험하다.
// 클립만 넷째 인자(해상도)를 탄다. 생략·모르는 값·해상도를 안 받는 모델은 videoPrice 와
// 같은 규칙으로 720p 칸에 떨어진다(여기도 화면이 부르는 자리라 던지지 않는다).
export function regenPrice(kind, priorCount, model, resolution) {
  const entry = REGEN_PRICE[kind];
  if (entry === undefined) throw new Error(`모르는 재생성 종류: ${kind}`);
  let p = entry;
  if (typeof p !== "number") {
    // priceModel 은 VIDEO_PRICE 를 보고 고른다 — 두 표가 갈리면(한쪽에만 모델을 더하면)
    // 여기가 undefined 가 되고 다음 줄이 TypeError 다. 이 함수는 화면(⑤영상)이 부르는
    // 자리라 던지면 페이지가 통째로 안 뜬다 — videoPrice 와 같은 원칙으로 옛 모델까지
    // 한 겹 더 받는다. (갈린 것 자체는 tests/pricing.test.js 의 대조가 잡는다.)
    const byRes = p[priceModel(model)] || p[LEGACY_MODEL];
    p = byRes[resolution] ?? byRes[PRICE_DEFAULT_RESOLUTION];
  }
  return Number(priorCount) >= FREE_REGEN_PER_CUT ? credits(p) : 0;
}

// 크레딧을 한 번도 못 받은 사장님이 **체험으로** 써 볼 수 있는 원가 한도(USD, 누적).
//
// 대화·브리핑·대본까지는 크레딧 없이 만들어 볼 수 있게 열어 뒀다 — 결과를 봐야 지갑을
// 연다. 대신 무제한이면 그대로 우리 돈이라 누적 상한을 건다. 편당 LLM 원가가 ~$0.06 이니
// 대본 여덟 편쯤이다.
//
// ★ env 가 아니라 여기 있는 이유: env 는 프로덕션에 넣는 것을 잊으면 조용히 기본값으로
// 돈다. 실제로 전역 예산 상한($20)이 그렇게 킬스위치가 될 뻔했다. 이건 가격 성격의
// 정책값이라 가격표에 둔다.
//
// ★ "하루 리셋"이 아니라 **누적**이다. 매일 리셋하면 영영 무료로 쓰는 사람이 생기고,
// 기간으로 재려면 sum_costs 에 기간 인자를 더해 **라이브 스키마를 다시 올려야 한다.**
export const FREE_TRIAL_USD = 0.5;

// ── 광고 경로(lib/ad) ────────────────────────────────────────────────────
// 표를 따로 둔다. 기존 VIDEO_PRICE 와 섞으면 같은 "15초"가 두 뜻이 된다 —
// 기존은 컷 여러 개를 합친 15초이고, 이쪽은 Seedance 클립 하나다. 원가가 다르다.
//
// ★ Task 21 — 길이가 모델에 딸린다(2.0 은 15초 하나, 2.5 는 15·30초 둘).
//   그래서 표가 모델 id → 길이 → 크레딧의 2단이다.
//
// ★ Task 24 — 해상도가 셋째 축이 됐다. 칸(모델×길이)의 값이 **숫자**면 해상도 무관
//   단일가(지금은 fast 티어 하나 — 아래 "판단" 참고), **객체**(해상도 → 크레딧)면
//   해상도별로 갈린다. 두 모양을 다 받는 이유는 tests/ad-pipeline.test.js(다른
//   태스크가 병행 중이라 건드릴 수 없다)가 `AD_VIDEO_PRICE["seedance-2.0-fast"][15]`를
//   **숫자 그대로** 빼 쓰기 때문이다 — fast 칸을 객체로 바꾸면 그 파일이 깨진다.
//
//   키는 lib/ad/models.js 의 AD_MODELS[].id·seconds·resolutions 와 **같아야 한다** —
//   이 파일은 화면도 import 하므로 models.js 를 import 해 대조할 수 없다(아래 규율 참고).
//   tests/pricing.test.js 가 AD_MODELS 를 훑어 이 표와 어긋나면 실패하게 대신 대조한다.
//
// 원가 근거(2026-08-13, calc = 계산값·2.5 와 2.0 480p·1080p 는 실측 아니다):
//   2.0-fast   720p $0.2419/s 고정(실측)         × 15초 = $3.63  → 65 크레딧(기존값 유지)
//   2.0        720p $0.3034/s(calc, t2v 값)      × 15초 = $4.55  → 80 크레딧
//   2.0        1080p $0.682/s(calc)              × 15초 = $10.23 → 175 크레딧
//   2.0        480p $0.1348/s(calc, 픽셀비 추정) × 15초 = $2.02  → 35 크레딧
//   2.5        720p $0.4622/s(calc, 토큰식)      × 15초 = $6.93  → 120 크레딧(기존값 유지)
//   2.5        720p $0.4622/s(calc)              × 30초 = $13.87 → 240 크레딧(기존값 유지)
//   2.5        480p $0.2056/s(calc, 토큰식)      × 15초 = $3.08  → 55 크레딧
//   2.5        480p $0.2056/s(calc)              × 30초 = $6.17  → 110 크레딧
// 1크레딧 ≈ 원가 $0.06 로 올림(5 크레딧 단위로 반올려 올린다 — 기존 65·120·240 값과
// 같은 자리수라 표가 어수선하지 않다). 올려 잡는 방향이 안전하다 — 내려 잡으면
// 팔수록 손해다.
// 2026-08-21 에 더한 것(같은 규칙: 1크레딧 ≈ 원가 $0.06, 5크레딧 단위로 올림):
//   2.5   1080p $1.0400/s(calc, 토큰식) × 15초 = $15.60 → 260 크레딧
//   2.5   1080p $1.0400/s(calc)         × 30초 = $31.20 → 520 크레딧
//   H3    2K    $0.13/s(fal 공시)       × 15초 = $1.95  → 35 크레딧
//   H3    4K    $0.16/s(fal 공시)       × 15초 = $2.40  → 40 크레딧
// ★ 2.5 의 1080p 는 **관리자 전용**이지만 가격은 반드시 있어야 한다 — 관리자가 만들어도
//   청구·기록은 똑같이 돌아야 하고(사장님 요구: "모든 가격은 정확히 표기 및 트래킹"),
//   값이 없으면 adVideoPrice 가 던져 그 자리에서 화면이 죽는다.
export const AD_VIDEO_PRICE = {
  // ★ 480p 15초를 35 → 40 으로 올렸다(2026-08-27, 1회 생성 기준 재계산). 원가
  //   $2.02 + LLM $0.20 = $2.22 라 35 크레딧($2.10)이 **원가를 밑돌았다**.
  //   나머지 칸은 그대로다 — 이 흐름에는 그림이 없어(단일 클립) 위 표의 ② 가 안 붙는다.
  "seedance-2.0": { 15: { "480p": 40, "720p": 80, "1080p": 175 } },
  // (fast 티어는 2026-08-13 에 걷어냈다 — 모델은 2.0 · 2.5 둘뿐이다)
  // ★ 1080p 를 260→265 · 520→525 로 올렸다(2026-08-28 머지). 받아온 값은 **클립 값만**
  //   보고 매겨졌는데, 이 저장소는 08-27 에 정가를 **1회 생성 원가**로 다시 재면서
  //   LLM $0.20(=3.3 크레딧)을 함께 넣었다 — 그래서 260 이 263.3 을 밑돌았다.
  //   판정은 tests/pricing-covers-cost.test.js 가 계산으로 한다(감이 아니다).
  "seedance-2.5": {
    15: { "480p": 55, "720p": 120, "1080p": 265 },
    30: { "480p": 110, "720p": 240, "1080p": 525 },
  },
  // ★ 해상도 키가 **대문자**다 — lib/ad/models.js 의 minimax-h3 resolutions 와 같은
  //   표기여야 한다(그 값이 그대로 fal 에 나가고 그대로 이 표를 조회한다).
  // ★ 같은 이유로 35→40 · 40→45(2026-08-28). H3 는 2K $0.13/s · 4K $0.16/s 라
  //   15초에 $1.95 · $2.40 이고, 거기에 LLM $0.20 이 얹힌다.
  "minimax-h3": { 15: { "2K": 40, "4K": 45 } },
};

// pricing.js 는 lib/ad/models.js 를 import 하지 않는다(화면 번들 규율) — 그래서 기본
// 모델 id 를 문자열로 다시 적는다. lib/ad/models.js 의 DEFAULT_AD_MODEL 과 **같은
// 값이어야 한다**(tests/pricing.test.js 가 대조한다).
const DEFAULT_AD_MODEL_ID = "seedance-2.0";

// 마찬가지로 lib/ad/models.js 의 DEFAULT_AD_RESOLUTION 을 문자열로 다시 적는다 — 같은
// import-금지 규율(tests/pricing.test.js 가 대조한다).
const DEFAULT_AD_RESOLUTION = "720p";

// 시나리오 다시 쓰기는 무료지만 무제한은 아니다. LLM 원가가 조금씩 샌다.
// MAX_REGEN_PER_CUT 과 같은 성격의 안전핀이다.
export const MAX_SCENARIO_TRIES = 20;

// modelId 를 **생략하면**(undefined/null — 옛 호출부·옛 문서) 기본 모델(2.0 standard) 값으로
// 본다. 화면(app/ads/[id]/page.js, Task 23 이 쓰는 중이라 이번 태스크에서 못 건드린다)도
// 이 1-인자(또는 2-인자, resolution 생략) 호출로 가격표를 읽는다 — 여기서 던지면 그 화면이
// 깨진다. 그래서 "생략"은 안전하게 받는다.
//
// ★★ 그러나 **뭔가를 줬는데 표에 없으면 던진다.** regenPrice 가 모르는 종류에 던지는
// 것과 같은 원칙이다 — 모르는 모델·해상도가 조용히 싼 값으로 떨어지면 그 차액이 그대로
// 우리 돈이다. "생략"과 "오타"를 가르는 이유: 생략은 옛 문서의 정상 상태이지만, 값이
// 있는데 표에 없다는 것은 라우트의 닫힌 목록 검사(app/api/ads/route.js 의 isAdModel·
// isAdResolution)를 어떻게든 피했거나 호출부가 버그로 잘못된 문자열을 넘겼다는 뜻이라
// 조용히 넘기면 안 된다. 해상도에도 **같은 원칙**을 그대로 적용한다.
export function adVideoPrice(seconds, modelId, resolution) {
  const key = modelId == null ? DEFAULT_AD_MODEL_ID : modelId;
  const table = AD_VIDEO_PRICE[key];
  if (!table) throw new Error(`모르는 광고 모델이에요: ${modelId}`);

  // 길이 — 그 모델의 목록 밖이면 **가장 비싼 값**으로 본다(기존 규칙 그대로). 싼 쪽으로
  // 떨어지면 원가보다 적게 청구할 위험이 있다(videoPrice 는 "중간값"으로 떨어뜨리지만,
  // 여기는 모델마다 길이가 최대 둘뿐이라 "비싼 쪽"이 더 안전하다).
  let cell = table[Number(seconds)];
  if (cell === undefined) {
    const knownSeconds = Object.keys(table).map(Number);
    cell = table[Math.max(...knownSeconds)];
  }

  // 해상도 무관(칸이 숫자) — 해상도별 문서가 없는 모델(지금은 fast 하나)은 단일가라
  // resolution 인자를 그냥 무시하고 그 값을 돌려준다.
  if (typeof cell === "number") return cell;

  // 해상도별(칸이 객체) — 생략하면 720p(옛 문서·옛 호출부와 같은 값), 값이 있는데
  // 그 모델·길이가 안 받는 해상도면 던진다.
  const resKey = resolution == null ? DEFAULT_AD_RESOLUTION : resolution;
  const p = cell[resKey];
  if (typeof p === "number") return p;
  if (resolution == null) {
    // 표의 모든 칸에 기본 해상도(720p) 값이 있어야 한다는 내부 불변식이 깨졌다 — 그래도
    // 안 죽게 가장 비싼 값으로 방어한다(위 "길이" 주석과 같은 안전한 방향).
    return Math.max(...Object.values(cell));
  }
  throw new Error(`이 모델은 그 해상도를 지원하지 않아요: ${resolution}`);
}
